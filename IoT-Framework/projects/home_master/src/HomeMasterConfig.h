// ============================================================================
// HomeMasterConfig.h — СХЕМА КОНФИГУРАЦИИ ПРОФИЛЯ (поля master.*, sd.*)
// ============================================================================
// Веб-UI строится автоматически по группам/меткам этой таблицы (админская
// страница ядра), как у smart_lock.
//
// Чего здесь НЕТ (владеет ядро, профиль не дублирует):
//   · сеть/MQTT/время/OTA   -> net.*, mqtt.*, sys.*, ota.*;
//   · ПИН администратора    -> AuthService (не конфиг вовсе).
//
// NB: mqtt.* мастера — это КЛИЕНТ к вышестоящему брокеру (режим BRIDGE, M2).
// Параметры ВСТРОЕННОГО брокера (режим SOLO) появятся на M1 отдельной
// группой broker.*.
// ============================================================================
#pragma once

/// Регистрация всех полей профиля в ConfigService.
/// Вызывается из HomeMasterApp::registerExtensions().
void registerHomeMasterConfig();

// ============================================================================
// test_rig.ino — ТЕСТОВЫЙ СТЕНД ЯДРА (разработка/регрессии МикроОС 5.0)
// ============================================================================
// Профиль TestProfile: зонды событий, conformance-стенд D1 с умышленным
// конфликтом пинов (стенд обязан его поймать), тестовый SoundPack/UI.
// Для боевого устройства — проект projects/smart_lock.
// ============================================================================
#include <MicroOS.h>
#include "src/TestProfile.h"

void setup() {
    Kernel::getInstance().run<TestProfile>();
}

void loop() {
    Kernel::getInstance().loop();
}

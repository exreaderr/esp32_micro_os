# Комплект: home_master 0.6.7 — ручная загрузка OTA + otam.src + host в манифесте

**Ядро:** МикроОС 5.8.6 (без изменений — замок и шлюз НЕ трогаем).
**Профиль:** home_master 0.6.6 → **0.6.7**. OtaMirrorService 0.1.1-om2 → **0.2.0-om3**.

## Что нового (сценарий «соседа», владелец 07.09.2026)

### 1. Ручная загрузка комплекта прошивки в зеркало (вкладка «Бэкапы»)

Карточка «Загрузить обновление вручную»: выбрать устройство, указать
version.json + firmware.bin + littlefs.bin (можно по одному, в любом порядке)
— мастер принимает файлы в staging на SD и «в строй» ставит только ПОЛНЫЙ
СОГЛАСОВАННЫЙ комплект. Проверки против человеческого фактора:

1. **Привязка к устройству:** поле `host` манифеста == выбранному устройству
   (иначе «ОТКЛОНЕНО: комплект для "weather_gate", а выбрано "smart_lock"»);
2. **md5 прошивки и ФС** против fw_md5/fs_md5 манифеста (битые/чужие файлы);
3. **Метка версии:** тег MICROOS|x.y.z|END внутри firmware.bin == version
   манифеста (манифест от одной версии, бинарник от другой).

Отклонённый комплект удаляется из staging целиком, зеркало продолжает
раздавать ПРЕЖНЮЮ целую тройку. Принятая тройка вытесняет старую в
`/ota/<host>/archive/` — «загрузил не то» обратимо.

Приём идёт на порту :8123 с админ-токеном: чтение раздачи для флота — по-
прежнему открытое, ЗАПИСЬ — только по токену. CORS для панели (:80 → :8123)
учтён.

### 2. otam.src — источник-переопределение (группа «OTA-зеркало», живое)

Пусто (по умолчанию) — как раньше, с HA. Задано (напр.
`http://192.168.1.10:8123`) — мастер зеркалирует с этого адреса: сценарий
«просто компьютер с веб-сервером» без HA вообще. Структура путей та же:
`/local/ota/<хост>/`.

### 3. Build Master: поле "host" в version.json

`conveyor/build_ota.sh` теперь пишет в манифест `"host": "<имя папки
зеркала>"` — это и есть подпись привязки для проверки №1. Старые манифесты
без host мастер принимает (проверка №1 просто не срабатывает), но все новые
релизы будут подписаны.

## Как обновляться

1. Раскатать файлы в клон репозитория (структура сохранена) + build_ota.sh
   в conveyor на компьютере сборки.
2. Менялись и прошивка, и ФС — **шьём ПАРОЙ** (прошивка + LittleFS).

## Проверка

1. Вкладка «Бэкапы»: появилась карточка «OTA-зеркало парка» (она пропадала —
   восстановлена) и «Загрузить обновление вручную».
2. Загрузить тройку smart_lock (из ~/Arduino/ota/smart_lock): вердикт
   «ПРИНЯТО: версия …», в карточке зеркала версия/размеры обновились.
3. Контрпроверка: ту же тройку загрузить, выбрав weather_gate →
   «ОТКЛОНЕНО: комплект для "smart_lock"…» (сработает, когда манифест со
   свежим полем host; на старом манифесте — отклонит по md5/версии, если
   файлы перепутаны).
4. Загрузить заведомо битый файл (обрезать firmware.bin) → «ОТКЛОНЕНО: md5…».
5. Штатный путь не сломан: «Проверить сейчас» в зеркале — опрос HA,
   устройство обновляется с мастера.

---

## ПРАВКА 1 (приёмка 07.09): «version.json: auth» — загрузки отклонялись по 401

**Корень:** ESP32-WebServer собирает только ЯВНО объявленные заголовки;
`X-Auth-Token` не был объявлен через `collectHeaders()` на сервере :8123 →
токен до handler'а не доходил → 401 «auth» на любом файле. (Отклонения,
увиденные при прогоне неподписанных комплектов, были этой же ошибкой —
проверки комплекта до дела не доходили.)
**Правка:** `collectHeaders(X-Auth-Token)` в attachRoutes + панель дублирует
токен в query-аргументе (двойной канал). Прошивка + ФС — парой.
**Повторная проверка:** пп. 2–4 основного паспорта (ПРИНЯТО / ОТКЛОНЕНО
по host / ОТКЛОНЕНО по md5) — теперь доходят до настоящей верификации.

---

## ПРАВКА 2 (07.09): bk.admin_pin — пароль лежал открытым текстом → в NVS

**Дырочка:** поле было объявлено `ConfigType::SECRET`, но БЕЗ флага
`CFG_SECRET`. Урок: тип сам по себе ничего не делает — NVS-хранение,
маскировку в панели и исключение из config.json включает именно ФЛАГ.
Пароль администратора парка лежал в config.json и светился в панели.
**Правка:** флаг `CFG_SECRET | CFG_NONE` — значение живёт только в NVS,
в панели «задан (скрыт)», write-only. Аудит схемы: других незакрытых
секретов нет (broker.pass закрыт в 0.6.6, больше SECRET-полей без флага
не найдено).
**Разовое действие после прошивки:** старое значение жило в config.json
и в NVS само не переедет — ПИН нужно ввести заново (Настройки → Бэкапы).
До ввода бэкапы устройств парка будут падать с «no_pin» — это ожидаемо.
**Прошивка:** только firmware (index.html не менялся этой правкой; если
ПРАВКА 1 ещё не прошита — шьём парой, она покрывает обе).






# ПАСПОРТ — home_master 0.6.8 «hm.fleet + гистерезис ПАЗ»

Дата: 07.09.2026. База: ядро 5.8.6 (НЕ тронуто, флот не прошивается).
Прошивается ТОЛЬКО мастер, и только firmware (index.html/data не менялись).

## Что нового

### 1. hm.fleet — присутствие парка в ПАЗ (копилка M5)
Закрывает дыру приёмки M3.3: «брокер видит отсутствие замка, а ПАЗ
здорова» — ПАЗ следил за МАСТЕРОМ, но не за парком.
- Ожидаемый состав: реестр bk.hosts (кроме self).
- Факт присутствия: активные MQTT-сессии брокера мастера
  (clientId устройства = его hostname, ядро так делает всегда).
- Хосты без известного hostname (ещё ни одного снимка) не проверяются:
  «нет данных» ≠ «молчит» — свежий мастер не поднимет ложную тревогу.
- Семантика (утверждена владельцем 07.09.2026):
  · bk выключен или имён нет → Ok (нет данных);
  · ЧАСТЬ парка молчит → Warning с именем первого молчащего;
  · молчат ВСЕ → Critical «молчит весь парк (N)» — это похоже на
    сломанный брокер/сеть, ситуация защитная.
- В панели появляется во вкладке ПАЗ автоматически (через /api/health) —
  UI не менялся. Это же — будущий корм третьего пикселя светофора.

### 2. Гистерезис доменных проверок ПАЗ (вариант Б)
Долг из приёмки M3.3: hm.bk клацал Warning/recovered на «мигающем»
хосте (offline→ok→offline), счётчик warn рос на шуме (1/мин).
- Беда признаётся бедой после 3 подряд плохих прогонов (~3 мин при
  прогоне 60 с), восстановление — после 2 подряд хороших (~2 мин).
- Применено к hm.bk, hm.ota и hm.fleet (общий ConfirmState в профиле).
- Ядро НЕ тронуто: при переезде флота на 5.8.7 механизм переедет в
  HealthMonitor (общий для всех проверок), тогда же порог «молчит»
  уйдёт в конфиг (решение владельца 07.09.2026). Из профиля — уберём.

## Файлы комплекта
- projects/home_master/src/HomeMasterHealthChecks.cpp — ConfirmState,
  гистерезис hm.bk/hm.ota, новая проверка hm.fleet;
- projects/home_master/src/BackupService.h/.cpp — fleetSilence()
  (сверка реестра с сессиями брокера), версия 0.3.1-m37;
- projects/home_master/src/HomeMasterApp.h — версия 0.6.8.

Сборка: clean, 1129100 Б (86%) flash / 110445 Б (33%) RAM.

## Приёмка
1. Прошить ТОЛЬКО firmware мастера (FS не трогать).
2. Вкладка ПАЗ: домен hm.fleet в списке, статус Ok при живом парке.
3. Отключить замок от сети (или выключить):
   · первые ~2 минуты — тишина (подтверждение), счётчик warn НЕ растёт;
   · через ~3 мин — hm.fleet: Warning «smart_lock молчит», warn +1 ОДИН раз.
4. Вернуть замок: через ~2 мин — recovered, счётчик уменьшился.
5. Дёрнуть замок кратко (<3 мин молчания): тревоги быть не должно вообще.
6. Отключить ВСЕ устройства парка: через ~3 мин — hm.fleet Critical
   «молчит весь парк (2)», журнал паник, critical +1.
7. Шум: оставить хост «мигать» — переходов не чаще, чем раз в ~5 мин
   (3 мин беда + 2 мин восстановление).
   
   
   # ПАСПОРТ КИТА — МикроОС 5.8.8 / home_master 0.6.10 «Двухполевая версия OTA»

Дата сборки: 08.09.2026
Основание: одобрено владельцем 08.09.2026 («Да, два поля более универсально…
Метку расширяем сразу. Делаем сейчас»).

## Что в ките

- `micro-os-5/src/` — ядро 5.8.8 целиком (источник истины, структура репозитория).
- `micro-os-5/projects/home_master/` — профиль мастера 0.6.10 целиком (вкл. data/).
- `бинарники/` — firmware.bin + littlefs.bin + version.json (манифест с
  НОВЫМ полем `profile_version`).

## Ядро 5.8.8 — двухполевая версия OTA

1. **Version.h**: `MICROOS_VERSION_STR "5.8.8"`. Если профиль объявил
   `#define MICROOS_PROFILE_VER x.y.z` ДО включения ядра (шапка App.h),
   в .bin зашивается ПОЛНАЯ метка `MICROOS|ядро|профиль|END` рядом с
   ядерной `MICROOS|ядро|END`. Проверено grep'ом по готовым образам —
   обе метки на месте во всех трёх профилях.
2. **UpdateService**: `setProfileVersion()` (профиль зовёт в init из
   `getVersion()`), сверка манифеста: обновление есть, если ядро новее
   ИЛИ (ядро равно И profile_version новее). Даунгрейд по ЛЮБОМУ полю не
   предлагается. Манифест без profile_version — сравнение только по ядру
   (обратная совместимость). `/api/ota/info` отдаёт `profile` (локальный)
   и `remote.profile_version`.
3. **OtaMirrorService**: читатель меток сканирует ВСЕ вхождения `MICROOS|`,
   предпочитает 4-полевую, 3-полевая — запасная. Приём комплекта: если в
   манифесте есть `profile_version`, метка в прошивке ОБЯЗАНА совпасть —
   иначе `reject:fw_prof:<tag|none>`.
4. **Панель мастера**: карточка OTA показывает «ядро · профиль» с двух
   сторон, различает «Доступно обновление ядра!» / «…профиля!»; вердикт
   `reject:fw_prof` переведён на русский.

## Профиль home_master 0.6.10

- `MICROOS_PROFILE_VER 0.6.10` в шапке HomeMasterApp.h — единственный
  источник версии профиля; getVersion() отдаёт тот же макрос строкой.
- init(): `setProfileVersion(getVersion())` + boot-лог `bin tag: …`
  (живая ссылка, чтобы --gc-sections не выбросил полную метку — урок 5.8.8).

## Конвейер (build_ota.sh, обновлён в общем хранилище)

Манифест version.json теперь получает поле `profile_version` — конвейер
извлекает значение макроса MICROOS_PROFILE_VER из `projects/<host>/src/*App.h`.
Нет макроса — поле пустое, сверка только по ядру.

## Порядок приёмки (ядро затронуто — перешивается ВЕСЬ флот)

1. Мастер — ВРУЧНУЮ и ПЕРВЫМ (он обслуживает OTA флота), fw+FS парой.
2. Замок и шлюз — OTA с мастера (тройки из `бинарники/` соответствующих
   китов; у каждой version.json со своим host — wrong_host не даст ошибиться).
3. Конфиги — из бэкапов.

## Проверки приёмки 5.8.8

- Панель мастера: «Текущая прошивка 5.8.8 · профиль 0.6.10».
- Boot-лог: `bin tag: MICROOS|5.8.8|0.6.10|END`.
- «Проверить обновления» при удалённом 5.8.7 → ветка «Доступно обновление
  ядра!» (впервые живая проверка ветки «Доступна прошивка!» — она же была
  мёртвая до правки update_available 07.09.2026).
- Негатив: тройка с чужим profile_version в манифесте → `reject:fw_prof`.
- Негатив: тройка с ядром 5.8.7 в метке → `reject:fw_ver` (не регрессия).

## Урок 5.8.8 (в копилку)

`__attribute__((used))` НЕ спасает строку от `--gc-sections` без живой
ссылки: локальный volatile-указатель LTO выкидывает. Рабочая ссылка —
передача адреса во внешний вызов (printf/log). Ядерная метка выживала
именно потому, что Kernel.cpp её печатает; полная метка без ссылки молча
исчезала из .bin (поймано grep'ом образа при сборке).






# ПАСПОРТ КИТА — МикроОС 5.8.9 / home_master 0.7.0 «M5: статус-LED + OLED»

Дата сборки: 08.09.2026
Основание: дизайн и распиновка утверждены владельцем 08.09.2026.

## Что в ките

- `micro-os-5/src/` — ядро 5.8.9 целиком (структура репозитория).
- `micro-os-5/projects/home_master/` — профиль 0.7.0 целиком (вкл. data/).
- `бинарники/` — firmware.bin + littlefs.bin + version.json.
- Новые библиотеки для стейджинга сборки: **Adafruit_NeoPixel**,
  **Adafruit_SSD1306**, **Adafruit_GFX**, **Adafruit_BusIO** — положить
  в `~/Arduino/libraries/` (или в libraries стейджа Build Master-а).

## Ядро 5.8.9 — читальный снимок проверок ПАЗ

HealthMonitor: публичные read-only акцессоры `checkCount()`,
`checkNameAt(i)`, `checkStatusAt(i)`, `checkMsgAt(i)`,
`checkLastRunAt(i)` — внешние индикаторы (LED/OLED) отображают ПАЗ,
не вмешиваясь в механизм. Чисто аддитивно, поведение ядра не менялось.

## Профиль home_master 0.7.0 — M5

### StatusLedModule (0x1109) — 4 пикселя WS2812 = лицо ПАЗ
- GPIO21, питание 3.3V (порог data 2.31V — чисто по спецификации),
  резистор ~470 Ом на data.
- Маппинг пикселей В КОНФИГЕ: `led.map` = `hm.sd,hm.bk,hm.ota,hm.fleet`.
- Цвета: зелёный Ok / жёлтый Warning / красный Critical / синий пульс —
  проверка ещё не бежала («нет данных»); погашен — `led.enabled=0`.
- Конфиг (группа «Статус-LED»): led.enabled (умолч. **false**), led.pin
  (21), led.count (4), led.bright (15%), led.map.

### PanelDisplayModule (0x110A) — экран по требованию
- OLED SSD1306 128×64 на I2C-шине платы (16/17 — та же шина, что DS3231;
  адрес 0x3C против 0x68 RTC).
- Экран ВСЕГДА погашен; будит любая кнопка; гашение по `oled.timeout`
  (умолч. 30 с). Кнопки +/−/ОК на GPIO15/18/2 (INPUT_PULLUP, активный
  низ, антидребезг 40 мс, длинное ОК ≥1.5 с).
- Меню: список проверок ПАЗ (человеческие имена, зеркало CHK_NAME
  веб-панели), ОК — детали (статус, сообщение, возраст прогона).
- **View-only первый этап**; длинное ОК — слот действий (пока «ДЕЙСТВИЙ
  НЕТ», архитектура второго этапа заложена).
- Кириллица — свой мини-шрифт 5×7 (CyrFont5x7.h, 165 байт, строчные →
  прописные, Ё≈Е).
- Конфиг (группа «Экран»): oled.enabled (умолч. **false**), oled.addr
  (60), oled.timeout (30), btn.plus/minus/ok (15/18/2).

### Распиновка (утверждена 08.09.2026)
WS2812 data → GPIO21 · OLED SDA → GPIO17 · SCL → GPIO16 ·
кнопки +/−/ОК → GPIO15/18/2 (на GND) · питание ленты 3.3V · OLED 3V3.
Ловушки учтены: GPIO33–37 заняты OPI PSRAM, страппинг 0/3/45/46 обойдён,
W5500 (9–14) и SD (4–7) не тронуты.

## Приёмка

1. Мастер — вручную, fw+FS парой (ядро затронуто, мастер первым).
2. Замок — OTA с мастера (кит 5.8.9, профиль 0.5.15 без изменений).
3. Шлюз — уведомление ветке (ядро аддитивное, адаптация формальная).
4. Железо M5 на макетке: `led.enabled=1` → пиксели оживают;
   `oled.enabled=1` → кнопка будит экран, листаем ПАЗ.
5. Без железа (оба enabled=0) — мастер работает как прежде
   (проверка закона самодостаточности).
   
   
   
   
   
   # ПАСПОРТ КИТА — МикроОС 5.9.0 / home_master 0.8.0 «Единая инфо-карточка флота»

Дата сборки: 09.09.2026
Основание: дизайн единой карточки утверждён владельцем 09.09.2026
(имя по-русски вместо hostname, IP не публикуем — закрытая зона).

## Что в ките

- `micro-os-5/src/` — ядро 5.9.0 целиком (структура репозитория).
- `micro-os-5/projects/home_master/` — профиль 0.8.0 целиком (вкл. data/).
- `бинарники/` — firmware.bin + littlefs.bin + version.json.

## Ядро 5.9.0 — единая инфо-карточка флота

- **Новый публичный endpoint `/api/info_card`** (JSON): имя, версия ядра,
  версия профиля, статус MQTT, время работы (с), текущее время,
  до 2 профильных строк extras. Публичный — карточка живёт и на
  welcome-странице замка до входа по ПИНу; состав жёстко открытый,
  IP/hostname НЕ отдаётся (решение владельца).
- **Новое поле конфига `sys.name`** (группа «Сеть», в КОНЕЦ схемы):
  человеческое русскоязычное имя устройства, редактируется из настроек,
  NVS-персистентно. Пусто = дефолт профиля. Задумано под флот
  smart_light'ов: каждому своё имя.
- **IUiProvider (аддитивно, с дефолтами):**
  - `uiDisplayName()` — имя по умолчанию («Умный замок», «Домашний
    мастер», …);
  - `infoCardExtras(labels, values, max)` — до 2 пар «метка: значение»,
    только открытая информация.
  Профили на 5.8.x пересобираются БЕЗ правок (дефолты в базовом
  интерфейсе) и получают карточку с пустыми extras.

## Профиль home_master 0.8.0

- `uiDisplayName()` → «Домашний мастер».
- extras: «Режим» (мост ↔ HA / соло / брокер выкл) + «Клиентов MQTT: N».
- Панель: подвал заменён на единую карточку — имя заголовком, строки
  «Системная информация: МикроОС X», «Профиль: Y», «MQTT», «Время
  работы», extras, «Текущее время». Опрос `/api/info_card` раз в 15 с.

## Приёмка

1. Мастер — вручную, fw+FS парой (ядро затронуто, мастер первым).
2. Замок — OTA с мастера (кит 5.9.0 / 0.5.16 рядом).
3. Шлюз — уведомление ветке (Issue #4): ядро аддитивное, профильная
   реализация extras — спека в тексте уведомления.
4. Проверка: подвал панели показывает карточку; `curl /api/info_card`
   на любом устройстве; задать sys.name = «Мастер дома» → карточка
   и заголовок меняются после применения.
   
   
   
   
   
   # ПАСПОРТ КИТА — МикроОС 5.9.1 / home_master 0.8.0 «Сторож сети»

Дата сборки: 10.09.2026
Основание: Issue ветки weather_gate «два дефекта автономности (CC1101 +
сетевой стек)», 10.09.2026; решения владельца: net.dead_reboot=false
по умолчанию (осторожный путь), окно 10 минут, свёртка однотипных логов,
расширение аудита.

## Что в ките

- `micro-os-5/src/` — ядро 5.9.1 целиком (структура репозитория).
- `micro-os-5/projects/home_master/` — профиль 0.8.0 целиком (вкл. data/).
- `бинарники/` — firmware.bin + littlefs.bin + version.json.

## Ядро 5.9.1 — автономность и наблюдаемость

- **Сторож мёртвой сети.** Если линк и IP живы, но N минут нет ни одного
  успешного ping шлюза и нет MQTT-сессии — сеть признаётся мёртвой:
  Error в лог, публикация ошибки `NET_DEAD`, событие `NET_EVENT_DEAD`
  (зеркалится в MQTT), запись в аудит. Ребут — ТОЛЬКО если включить
  новое поле `net.dead_reboot` (по умолчанию ВЫКЛ — осторожный путь).
- **Новые поля конфига (группа «Сеть», в КОНЕЦ схемы):**
  - `net.dead_reboot` (BOOL, по умолчанию false) — программная
    перезагрузка при мёртвой сети;
  - `net.dead_min` (UINT, 3–120, по умолчанию 10) — минут молчания
    сети до вердикта «мертва».
- **Backoff на сбой создания ping-сессии.** Раньше при исчерпании
  сокетов lwIP попытка повторялась каждые ~500 мс бесконечно — спам
  `esp_ping_new_session(267): create socket failed: -1` часами (логи
  инцидента на шлюзе). Теперь при сбое создания сессии дедлайн
  переносится, плюс диагностический счётчик `_pingCreateFailStreak`
  (1-й и каждый 20-й сбой — строка со свободной кучей).
- **Свёртка однотипных логов.** Подряд идущие идентичные строки
  (уровень+тег+тело) схлопываются в одну с суффиксом «(×N)» — журнал
  перестаёт заспамливаться криками, история читаема.
- **Аудит: +поле `hp`** — свободная куча в КБ на момент события; новый
  тип события `NET_DEAD`.
- **CC1101 (для шлюза):** принудительный аппаратный сброс по SPI
  (ручной SRES: ожидание CHIP_RDYn на MISO, строб 1 МГц, readback
  PARTNUM/VERSION с FAIL-логом) — восстановление после тёплой
  перезагрузки без передёргивания питания; сторож MarcState раз в 60 с
  (выход из RX → полная переустановка приёма, счётчик rfStateFixes).

## Профиль home_master 0.8.0

Без изменений относительно 0.8.0 кита 5.9.0 — пересобран на ядре 5.9.1.
Метка в бинарнике: `MICROOS|5.9.1|0.8.0|END` (+ короткая `MICROOS|5.9.1|END`).

## Приёмка

1. Мастер — вручную, fw+FS парой (ядро затронуто, мастер первым).
2. Замок — OTA с мастера (кит 5.9.1 / 0.5.16 рядом).
3. Шлюз — отдельное уведомление ветке: ядро 5.9.1 отвечает на оба
   пункта их Issue, профиль трогать не нужно.
4. Проверка на стенде: выдернуть патч-корд из коммутатора на 10+ мин
   (или отключить upstream) → в журнале диагностика ping-create-fail
   со свёрткой, по истечении окна — `NET_DEAD` в логе/аудите/MQTT,
   устройство НЕ ребутается (дефолт). Вернуть сеть — RECOVERED штатно.

// ============================================================================
// smart_counter.ino — УМНЫЙ СЧЁТЧИК (МикроОС 5.0, WT32-ETH01)
// ============================================================================
// Профиль SmartCounterProfile: счётчики воды/газа (импульсы, PCNT),
// электросчётчик Меркурий-206 (RS-485), тарификация, фильтр Big Blue,
// АКБ ИБП. Обязательные элементы системы: WT32-ETH01 + DS3231 (время,
// RTC=UTC) + супервизор питания MAX803S (линия EN, страж flash от
// «умирающих записей»).
//
// Стадии: C0 — концепт-нота (ПРИНЯТА ядерной веткой 01.09.2026, Issue #3);
// C1 — скелет + импульсный ввод (ScPulseCore + host-тесты, стенд);
// ядро 5.8.8: двухполевая версия OTA (MICROOS_PROFILE_VER в App.h);
// панель /web/sc.html (дизайн — наследие монолита v4.4.2);
// C2 — учёт и персистентность; C3 — полный профиль (UI правило 23,
// HA discovery, ПАЗ); C4 — интеграции + soak 72 ч.
// Host-тесты чистой логики — host/tests.cpp (./run_tests.sh).
// ============================================================================
#include <MicroOS.h>
#include "src/SmartCounterProfile.h"

void setup() {
    Kernel::getInstance().run<SmartCounterProfile>();
}

void loop() {
    Kernel::getInstance().loop();
}

# weather_gate — дорожная карта проекта

## История вопроса (происхождение названия)

Владелец, имея хорошую погодную станцию с уличным блоком и домашним монитором,
захотел подключить её к компьютеру — собирать, накапливать и анализировать
данные с уличного блока. Штатное ПО оказалось только под Windows и притом
«кривое»; забрать данные с домашнего монитора по USB не удалось. Тогда
возникла идея изготовить устройство, которое **само получает сырые данные
с уличного блока параллельно родному монитору**, обрабатывает их и отдаёт
дальше потребителям в удобоваримом виде. А что это, как не **шлюз**, через
который данные текут тому, кто их запрашивает — отсюда и имя: weather_gate.

Из этой предыстории выросли ключевые доктрины профиля: радио строго
receive-only (ничего не передаём в эфир, родной монитор не страдает),
открытые интерфейсы вместо проприетарного ПО (HTTP API, MQTT retained,
графики в браузере), накопление истории своими силами (DataLog).

---

Ведётся по ходу стадий W1–W5. Статусы: ✅ сделано, 🔧 в работе, 📋 запланировано, 💤 отложено (опция).

## Стадии

| Стадия | Содержание | Статус |
|---|---|---|
| W1 | Скелет профиля, Bme280Driver (BME280/BMP280, автодетект 0x76/0x77), конфиг wx.*, conformance | ✅ host 43598 PASS |
| W2 | Cc1101Driver (строго RX-only), декодер Fine Offset WH1080 (PCM 58 мкс, rtl_433-эталон), дедупликация пары 31 мс | ✅ host 43719 PASS |
| W3 | WeatherCore (feels-like, дождь, state), weather-JSON (HTTP + MQTT retained) для smart_lock, DataLog + uPlot-панель /web/wx.html, ПАЗ-проверки wg.bme280/wg.radio, авто-высота по координатам | ✅ host 43745 PASS, сборка 4/4 профилей |
| W4 | Зеркало даталога wx_* на home_master — **реализовано ядерной веткой** (WeatherMirror, релиз 5.8.2; контракт weatherJson подтверждён полем) | ✅ код; стенд — на владельце; релиз 5.8.2 после бенча зеркала |
| W5 | **Слоёный прогноз** (концепция одобрена владельцем 23.08): L1 — Замбретти + 5-классовый баротренд, офлайн всегда; шторм-предупреждение (>4 гПа/3 ч) событием на шину + warning в ПАЗ. L2 — внешний прогноз Open-Meteo, только FULL, с честной подписью источника. Плюс backfill зеркала и soak 72 ч | 📋 |
| W6+ | **Слой 3 — локальная ML-модель** на собственном архиве DataLog: обучение офлайн (ПК/мастер), крошечный инференс (регрессия/дерево). Триггер: накоплен архив за сезоны | 💤 стратегия |

## Опции (отложено решением владельца)

### 💤 Датчики среды технического помещения (котельной)
**Решение владельца (W3):** шлюз стоит в котельной — температура там всегда
повышена, влажность понижена; комнатные SHT31/VEML7700 смысла не имеют.
Зафиксировано как опция на будущее, в активный план не входит.
- **Триггер возврата:** появление задачи контроля микроклимата самой
  котельной (перегрев помещения, протечки → тогда кандидаты: SHT31 0x44
  на системной I2C 32/33, датчик протечки на свободный GPIO 5/12).
- **Стоимость входа:** драйвер по образцу Bme280Driver + поля конфига в
  конец схемы + host-тесты; ядро не затрагивается.

## OTA на погодном шлюзе (W3 fix2, 23.08.2026)

- **Запрос владельца:** OTA на шлюзе, как на мастере и замке (эталон —
  веб-страница smart_lock). Актуально при стендовых итерациях.
- **Анализ:** вся OTA-механика — ядерная (UpdateService: A/B-разделы с
  автооткатом, валидация 60 с, манифест на HA, фоновая докачка в tick,
  журнал истории, NVS-replay) и уже работает на шлюзе (19 модулей,
  UpdateService в списке, /api/ota/* под requireAdmin). Профилю нужна
  только панель.
- **Сделано (только UI, ядро и App не тронуты):** карточка «Система» в
  /web/wx.html после входа администратора — сведения OTA, проверка
  манифеста, обновление с сервера с следилкой прогресса, ручная загрузка
  .bin прошивки и ФС с прогресс-баром (предупреждение: ФС стирает
  даталог), NVS-бэкап/восстановление конфигурации, перезагрузка,
  ссылка на ядерную /admin. Версия профиля 0.3.0 → **0.3.1**.
- **Ворота:** host 43745/0 + ядерный 43351/0; сборка 4/4 —
  weather_gate 1075734 B (байт-в-байт fix1), соседние без изменений.
- **Развёртывание:** первый раз ФС с новой панелью — по кабелю (как
  заливался lock.html); дальше всё по воздуху.

## W5 принят ядром (Issue #1, 23.08.2026)

- Интерфейс `wxc::zambretti(pressSeaHpa, trend3hHpa, windDirDeg, summer)
  -> {letter, trend}` — **согласован**. Тренд из своего DataLog wx_p,
  сезон из RTC, `wx.zamb_en` в конец схемы — по доктрине.
- **Ядро ЗАРЕЗЕРВИРОВАЛО:** слот в реестре AuditService под
  `WG_EVENT_STORM_WARNING` + метрику HealthMonitor `wg.storm`
  (порог >4 гПа/3 ч). Слот наш, публикуем в реализации W5.
- Слой 2 (Open-Meteo): принят, только FULL, `wx.fcst_en=1` по умолчанию.
- Backfill: по умолчанию ВЫКЛ (`wx.mirror_backfill 0`); MVP — retained +
  публичный /api/dev/weather. Исторический backfill — не раньше 5.8.3,
  по отдельному решению владельца.
- Слой 3 (ML на P4): вектор зафиксирован, интерфейсов не создаём.
- **Soak 72 ч — общий протокол принят:** uptime, reboots, heap watermark,
  pkt/dup/crc_err, тиры DataLog, MQTT-переподключения, retry
  altitudeTask, стабильность HealthMonitor. **Мастер и замок обновлены
  до 5.8.2 и ушли на параллельный стенд** (владелец, 23.08); результаты
  сводим в Issue #1.

## Стратегическое будущее: аналитический узел на ESP32-P4

- **Планы владельца (23.08.2026):** мастер переезжает с ESP32-S3-ETH
  (SD 32 ГБ) на **ESP32-P4-WIFI6-POE-ETH** + апгрейд флеша до 32 МБ.
- P4: двухъядерный RISC-V 400 МГц, векторные AI-инструкции; радио
  на модуле через companion-чип. С SD и зеркалом wx_* мастер становится
  **озером данных** глубиной сезоны/год.
- Архитектурная роль: шлюз остаётся сенсорным краем (слои 1–2),
  мастер на P4 — аналитическим узлом слоя 3: хранение глубокого архива,
  обучение/подбор модели, раздача уточнённого прогноза обратно.
  МикроОС-философия сохраняется: при отказе аналитики край остаётся
  на слое 1 и не тупеет ни на грамм.
- Это мысли на будущее — в активный план не входит, триггер: приход
  железа P4 + накопленный архив.

## Кандидаты в ядерную ветку (запросы от профиля)

1. **library.properties: `depends=SPI,Wire`** (W3). Драйверы
   src/drivers/ безусловно включают `<SPI.h>`/`<Wire.h>`; без `depends`
   arduino-cli не даёт include-пути в профилях, где SPI не подтягивается
   ничем ещё (поймано сборкой test_rig). Однострочное изменение.
   **Статус: ПРИНЯТО ядерной веткой (Issue #1, п.3.1) — войдёт в 5.8.2.**
   Шаблонный library.properties из зоны профиля удалён (п.5 Issue #1).
2. Урок W1: кламп отрицательного давления в compensatePress (край ADC →
   wrap int64→uint32 давал ~16,7 МПа). Принят к сведению (Issue #1, п.3.2).

## Синхронизация с ядерной веткой (Issue #1, 18–19.08.2026)

- Единый репозиторий: `exreaderr/esp32_micro_os` (ядро 5.8.1,
  каноничная раскладка: `MicroOS/` + `projects/` — соседи). Канал
  связи — Issues основного репозитория.
- Ревью W1–W3: **приняты** после устранения блокера.
- **Блокер (п.4) УСТРАНЁН:** Cc1101Driver больше не включает
  WeatherGateProfile.h — пины параметром (`Cc1101Pins` + `configurePins()`
  до init, без configurePins — честный отказ); пины для ISR — файловые
  статики; инклуды ядра в драйверах переведены на угловые
  (переносимость между раскладками и будущий мерж в MicroOS/src/drivers).
- **W4 СОГЛАСОВАН (п.3.3):** модуль-зеркало даталога — на стороне
  home_master (retained MQTT /weather или опрос HTTP — выбор при
  реализации). Код gate не меняется; gate самодостаточен.
- Верификация после правок: host 43745 PASS / 0 FAIL; сборка
  weather_gate OK (1075734 B flash); контроль блокера — smart_lock /
  home_master / test_rig собраны с ИЗЪЯТЫМ из дерева
  projects/weather_gate (smart_lock байт-в-байт 1093730 B — их
  релизная цифра 5.8.1).

## Повторная приёмка и релиз 5.8.2 (19.08.2026)

- **W1–W3 fix1 — ПРИНЯТО** (комментарий ядерной ветки в Issue #1):
  блокер закрыт эмпирически, host 43745/0, сборка 4/4 из main.
  Расхождение flash −16 B от моих 1075734 при идентичных globals —
  шум путей сборки, зафиксировано для протокола с их стороны.
- **Релиз 5.8.2** содержит: `depends=SPI,Wire` (мой запрос п.3.1),
  WeatherMirror (W4) в home_master, мой fix1 байт-в-байт.
- **WeatherMirror (W4)** реализован по контракту weatherJson 1:1
  (проверено по исходнику: valid/temp/feels_like/state/humidity/wind/
  gust/dir/rain/press| null/press_sea/rssi/batt/age_s; batt: 1=ОК, 0=
  разряжена). Два канала приёма: subscribeExternal (норма) + хук
  локального брокера (автономия), дедуп по телу, каналы DataLog с теми
  же именами wx_ot/wx_oh/wx_p/wx_w/wx_r.
- Моя верификация 5.8.2 (повторная, со своей стороны): профильный
  host-сьют 43745 PASS / 0 FAIL; ядерный 43351 PASS / 0 FAIL; сборка
  4/4 профилей (weather_gate 1075734 B, smart_lock 1093730 B
  байт-в-байт, home_master 1123394 B, test_rig 1057770 B).
## Мердж драйверов (Issue #1, 20.08.2026)

- **П.3 ВЫПОЛНЕН ядерной веткой:** Bme280/Cc1101 + Core-логики живут в
  MicroOS/src/drivers/; профиль переключён на библиотечные драйверы
  (угловые инклуды, локальные src/drivers удалены). Пакет возвращается
  через владельца — приёмка с полным прогоном ворот после получения.
- Их ворота D3: host 43351+43745 PASS; сборка 4/4, weather_gate
  1075718 B — байт-в-байт fix1, соседние профили без изменений;
  мой host-сьют адаптирован ими к библиотечным путям (run_tests.sh
  работает как прежде).
- **Релиз 5.8.2 — после бенча зеркала (W4) на стенде.** W5 (Замбретти,
  backfill, soak 72 ч) объявлен следующим фронтом, ядро ждёт предложения
  по интерфейсу.

## Эррата платы WT32-ETH01: IO5/IO35

- **Прозвонка владельцем (20.08.2026, несколько плат):** пэд,
  подписанный шелкографией «IO35», — это фактический **GPIO35**
  (input-only, без подтяжек) — как и принято в пин-плане (CC1101 MISO).
- **Источник разночтения (расследование владельца):** опечатка в ранних
  официальных схемах Wireless-Tag (контакт назвали IO5, не имея
  отношения к GPIO5), разошедшаяся по неофициальным распиновкам.
  С версии платы v1.2 шелкография исправлена на IO35.
- **Свойства GPIO35:** строго вход (input-only), внутренних подтяжек
  в кристалле нет. Для CC1101 MISO — идеальное совпадение роли:
  линию активно драйвит трансивер, подтяжка не нужна.
- **Настоящий GPIO5** выведен на пэд **CFG** — учитывать, если он
  понадобится (опция датчиков котельной упоминает GPIO 5/12 для
  протечки: подтяжка у GPIO5 есть, валиден).
- **Изменений в коде и пин-плане НЕ требуется.** При сборке ориентир —
  фактическая прозвонка, а не надпись на плате или старые схемы.

## Стенд владельца (23.08.2026)

- **Шлюз в работе:** прошивка 5.8.2 (build ee57070), id d4e9f4bd612b,
  ETH 10.146.75.55, MQTT на встроенный брокер home_master (10.146.75.54).
- Радио — временный модуль E07-M1101D (основной ждёт плату-переходник);
  RSSI −108 дБм на времянке, внимания не обращаем до основного модуля.
- **Монитор порта — чисто:** 19 модулей, 4/4 драйвера init ok (cc1101
  включительно), конформанс 14 PASS / 0 FAIL на железе, RTC→NTP
  синхронизация, 5 каналов DataLog wx_* подняты.
- **Сверка с консолью WH1080:** давление BME280 и влажность — копейка
  в копейку; остальное идёт с уличного блока и живо обновляется в панели.
- Панель /web/wx.html живая (автообновление 30 с, карточка «Сейчас»).
- Монтажная находка: перепутанные SDA/SCL давали отказ DS3231+BME280 —
  штатный путь отлова: critical wg.bme280 + невалидное время. Исправлено.
- **ПАЗ — боевая проверка пройдена (владелец):** BME280_LOST (critical)
  на перепутанной шине → локализация по сообщениям сторожа → после
  перепайки самовосстановление critical→ok без перезагрузки. Переходы
  состояний работают как задумано.
- Координаты зафиксированы, высота над уровнем моря введена вручную
  (точнее авто-высоты по модели ~90 м); press_sea считается честно.
- **Стенд переведён в режим накопления статистики** (сверка 24 ч,
  далее бенч зеркала W4).
- **Косметика в бэклог:** имена каналов «Улица, температура/влажность»
  режутся по 28-байтному бюджету посередине кириллического символа —
  сократить названия при следующем пакете (профильная правка).
- Стороннее наблюдение (не наш профиль): у smart_lock в журнале мастера
  CPU_TEMP_CRITICAL 85,0 °C — передать ядерной ветке/проверить корпус.
- Далее: сверка приёма 24 ч, бенч зеркала W4 (каналы wx_* мастера vs
  шлюза), проверка авто-высоты (wx.lat/lon → wx.altitude_m).
- **24.08: установлен основной радиомодуль Ebyte E07-900M10S** — пакеты
  стабильные, RSSI нормализовался (−86 дБм против −108 на времянке
  E07-M1101D). Начат полноформатный прогон с накоплением данных.
- **24.08: панель OTA развёрнута и работает** (бэкап конфигурации в NVS
  сохранён владельцем, манифест-URL сформирован).
- **Заявка владельца (24.08):** перенос наработок веб-UI монолита
  v5.2/v5.3 PRO в новую панель; сканер АЧХ — строго в админскую часть.
  Предложения: weather_gate_веб_интерфейс_предложения.md (блок A —
  публичная страница, блок B — сканер после приёмки мерджа драйверов).

## Приёмка мерджа драйверов + W3.2 (24.08.2026)

- **Приёмка ПРОЙДЕНА.** Мердж уже в main репозитория (свежий клон):
  MicroOS/src/drivers/*.h/.cpp — **байт-в-байт** наши профильные файлы.
  Профиль переключён на библиотечные драйверы: локальные src/drivers
  удалены, инклуды переведены на угловые `<drivers/...>`, host-сьют — на
  пути ../../../MicroOS/src/drivers. Контроль ДО наложения W3.2:
  host 43745/0, сборка weather_gate **1075734 B — байт-в-байт fix2**
  (Δ+16 B к их 1075718 — тот же шум путей, что и на приёмке 5.8.2,
  зафиксировано для протокола).
- **WxTrend.h (новый профильный заголовок):** `wxc::baroTrend3h` вынесен
  из WeatherCore.h — после мерджа WeatherCore.h ядерный, профиль его не
  правит. Пороги тренда — профильная политика, живут в профиле.
- **W3.2 бэкенд (0.3.2):** статистика 24 ч из часового яруса DataLog
  (tmin24/tmax24/pmin24/pmax24/wmax24, обновление кэша раз в 300 с,
  писатель — только tick), баротренд 3 ч (`trend`: −1/0/+1, порог
  ±1.0 гПа — ниже шум BME280), `in_temp` (BME280 котельной).
  HTTP /api/dev/weather отдаёт weatherJsonFull; MQTT остаётся компактным
  (бюджет 256 Б, контракт замка/зеркала нетронут — расширения аддитивны).
  Имена каналов сокращены под 28-байтный бюджет («Улица, темп.» и т.п. —
  бэклог стенда закрыт).
- **W3.2 UI (заявка владельца «похоже и не похоже»):** публичная зона —
  герой (температура/состояние/мин-макс 24 ч), сетка метрикарточек со
  шкалами (влажность, давление мм рт.ст. 728–788, ветер 0–20 м/с, дождь),
  роза ветров (16 румбов, стрелка «куда дует»), карточка тенденции
  (↑/→/↓ за 3 ч), статус-блок (RSSI, возраст пакета, батарея, t°
  котельной). Админская зона — карточка «Навигация по настройкам» по
  концепции smart_lock (чипы с per-tab акцентами --tile, наша тёплая
  палитра): 📈 Графики / 📻 Радио / 📡 Сеть / 🛡️ ПАЗ / 🩺 Система /
  👑 Админ. ПАЗ-вкладка: /api/health — состояние, счётчики, куча, WDT,
  все проверки с бейджами OK/WARN/CRIT + журнал ПАЗ. Радио: счётчики
  CC1101 (pkt/dup/edges_dropped/RSSI/возраст). Сканер АЧХ — заглушка
  с честной подписью (W3.3, после дельты драйвера через Issue #1).
- **Ворота W3.2:** host 43751/0 (+6 CHECK баротренда) + ядерный
  43351/0; сборка 4/4 — weather_gate **1077190 B** (+1456 к fix2:
  статистика/тренд), smart_lock 1093730 / home_master 1123394 /
  test_rig 1057770 — без изменений.
- **Пакет:** weather_gate_W3.2.zip (12 файлов; драйверы теперь берутся
  из ядра 5.8.2 main — для сборки нужен свежий клон репозитория).
- **Развёртывание на стенде:** прошивка 0.3.2 и ФС — уже можно по
  воздуху (панель fix2 на шлюзе имеет загрузку .bin прошивки и ФС).
  Напоминание: обновление ФС стирает даталог — перед ним NVS-бэкап.

### W3.2 fix2 (24.08.2026): диагностика «пустых» графиков

- **Симптом (владелец):** уличные данные идут и отображаются, но графики
  пишут «Нет данных (HTTP 404)» — ощущение, что в базу не складывается.
- **Проверено по коду:** путь записи цел (logPoint по каждому пакету,
  давление — по тику BME280); диспетчер /api/dev/* отдаёт tail без query
  (проверено по исходнику WebServer 3.3.11); wgApiDlog идентичен рабочему
  apiDlog замка; единственный 404 внутри — no_channel (ch>=count), что
  противоречит работающему dlog/channels (5 каналов). Расследование
  продолжается — нужно ТЕЛО ответа, чтобы назвать слой отказа.
- **Сделано (только wx.html):** графики теперь показывают тело ошибки
  («HTTP 404: no_channel» / «unknown_dev_api» / «not_found») — один
  скриншот назовёт слой. 404 больше не выбрасывает из админки.
- Проверено headless-стендом: тело ошибки выводится, JS-ошибок нет.
  Прошивка не тронута; ворота 4/4 + host 43751/0 + 43351/0 — зелёные
  с fix1 (исходники прошивки не менялись).

### W3.2 fix1 (24.08.2026): вход-пасхалка + устойчивость входа

- **Замечание владельца:** вход в админку «моргал» и не открывался.
  Расследование: headless-стенд с заглушкой API показал, что логика
  входа исправна; «моргание» — это enterAdmin → тихий 401/404 на одном
  из админских эндпоинтов → leaveAdmin (возврат на вход) без диагностики.
- **Исправлено:** после получения токена — проверочный запрос
  /api/system ДО открытия админки; отказ → видимая ошибка с HTTP-кодом
  прямо в модалке («Токен не принят контроллером (HTTP N)»). 401 в
  любом админском запросе → явный выход с тостом «Сессия истекла»;
  прочие коды показываются внутри вкладки, без скрытого выхода.
  Сохранённый токен при открытии страницы тоже проверяется probe'ом.
  Оба сценария проверены в headless-браузере (успех / отказ токена).
- **Редизайн входа по образцу замка (заявка владельца):** публичная
  страница очищена — поля пароля и ссылки weather-JSON в видимой зоне
  больше нет. Нижняя плашка «weather_gate · МикроОС 5.0» — пасхалка:
  5 тапов подряд → модальное окно входа (✕/Esc/тап по фону — закрыть).
  Ссылка weather-JSON перенесена во вкладку «Админ».
- **Ворота:** host 43751/0 + ядерный 43351/0; сборка 4/4 без изменений
  (прошивка не тронута — менялась только ФС-страница wx.html).
- **Пакет:** weather_gate_W3.2_fix1.zip. На стенде достаточно обновить
  ФС по воздуху (прошивка 0.3.2 уже стоит).

## Уроки (нумерованные)

- **№12 (W2):** контрольные точки частотного слова CC1101 считать в той же
  арифметике, что и целевой код (float32 против double — 1 LSB).
- **№13 (W3):** host-тесты не покрывают App/Profile — только Core.
  Компиляционный прогон всех профилей arduino-cli обязателен до выдачи
  (поймано: регистрация Cc1101Driver без include; LogLevel::ERROR вместо
  LogLevel::Error; NetworkManager вместо NetworkService).
- **№14 (W3):** arduino-cli добавляет include-путь библиотеки, только если
  её заголовок виден в цепочке скетча; драйверы в общей src/drivers —
  повод держать `depends` в library.properties актуальным.
- **№15 (W3-fix, Issue #1):** общий драйвер НЕ включает заголовки профиля —
  зависимости передаются параметрами (configurePins до init). Относительные
  quoted-инклуды в переносимых файлах — мина: резолвятся «на глубину»
  каталога и тихо ломаются при смене раскладки; для переносимых драйверов —
  только угловые инклуды ядра. Контрольный тест: сборка соседних профилей
  с ИЗЪЯТЫМ из дерева собственным профилем.
- **№16 (W3.2, приёмка мерджа):** после передачи файла в ядро профиль его
  больше НЕ правит — даже «свои» наработки. Профильные расширения (пороги,
  помощники) выносятся в отдельные профильные заголовки (WxTrend.h) заранее,
  до мерджа, — тогда приёмка сводится к смене инклюдов. Эталон приёмки:
  сборка ДО наложения новых правок должна дать байт-в-байт прежнюю прошивку
  (1075734 = fix2) — чистая атрибуция любой дельты.

---

## W3.2-diag1 — диагностическая сборка (ловля 404 графиков + заморозки RSSI)

**Дата:** 25.08.2026. **Версия:** `0.3.2-diag1`. **Пакет:** `weather_gate_W3.2_diag1.zip`.

**Повод:** графики на стенде — «Нет данных (HTTP 404)», данные в интерфейсе
идут; попутно — заморозка RSSI после ребута (−93 дБм, не критично).
Статический анализ исчерпан: query в `uri()` не входит (доказано исходником
WebServer 3.3.11), диспетчер идентичен замку, у замка графики работают →
ядро исправно, затык в пути weather_gate. По предложению владельца —
временное расширенное логирование вместо гадания.

**Что добавлено (только профиль, ядро не тронуто):**
- Конфиг `wx.diag` (bool, по умолчанию вкл.) — **в конец** группы
  «Телеметрия и сторожа» (правило схемы). Отключается из /admin без
  перепрошивки — после локализации бага логирование глушится галочкой.
- Публичный мост `WeatherGateApp::logDiag(const char*)` + свободный
  `s_diagLog(fmt, ...)` — `ModuleBase::log` protected, а обработчики UI —
  свободные функции (урок №17 ниже). Бюджет строки 92 Б под
  `LOG_BODY_LEN=96`.
- Точки лога (все с префиксом `DIAG`):
  1. `dlog init: ids=… count=… heap=…` — результат регистрации 5 каналов
     при старте (если ids=-1 — канал не встал, 404 закономерен);
  2. `api '<tail>': tok=… admin=…` — каждый запрос dlog*: токен только
     «есть/НЕТ» (сам токен в лог НИКОГДА), результат admin-check
     (отсечёт гипотезу «профильный путь теряет админский токен»);
  3. `dlog: ch='…'->N count=M range='…'` — вход в wgApiDlog ДО проверок
     (точное значение ch/count в момент 404);
  4. `dlog: raw/tier ch=N n=… dropped=…` — итог выборки + счётчик
     отброшенных точек канала (dropped растёт ⇒ время недостоверно);
  5. `wr: rc=TTTT unix=… pkt=… dup=… edges=… rssi=…` — на каждый
     принятый пакет: успех 4 logPoint + радио-счётчики (rc=0 ⇒ точка
     отброшена; rssi/edges — по заморозке уровня);
  6. `wr press: rc=0 …` — только отказы записи давления (раз/мин).

**Ворота:** host 43751 PASS / 0 FAIL (норма держится), ядерные
43351 PASS / 0 FAIL, сборка 4/4 (weather_gate 1078298 Б, smart_lock
1093734, home_master 1095468, test_rig 1057770).

**Снятие лога владельцем:** прошить по ОТА → открыть вкладку «Графики»
(спровоцировать 404) → прислать хвост журнала: /admin → журналы, либо
`/api/logs`, либо serial. Искомые строки — с префиксом `DIAG`. После
локализации: `wx.diag=0` в /admin, решение о постоянном логе — отдельно.

**Урок №17 (diag1):** `ModuleBase::log` — protected: свободные функции
UI-провайдера логируют через публичный мост модуля с предформатированной
строкой (varargs-обёртка protected-метода невозможна без наследования).
Диагностические переключатели — конфигом в конец схемы, не #define:
снятие лога с устройства не должно требовать перепрошивки.

---

## W3.2-diag2 — результаты диагностики 404 + исправление probe-точки входа

**Дата:** 25.08.2026. **Версия:** `0.3.2-diag2`. **Пакет:** `weather_gate_W3.2_diag2.zip`.

**Что показал diag1 (serial-лог стенда):**
1. **Данные в базу СКЛАДЫВАЮТСЯ.** Каналы регистрируются (ids=0..4, count=5,
   heap 140 КБ), каждый пакет пишется: `rc=1111`, unix валиден
   (RTC+NTP). «Не складываются» — опровергнуто, даталоггер здоров.
2. **Механизм 404 пойман:** в 16.4 с после бутa запрос `dlog/channels`
   пришёл с токеном, но `admin=NO` → 404 `unknown_dev_api`. Это стухший
   токен: сессии — RAM-таблица ядра и умирают с ребутом/OTA, а
   sessionStorage браузера — нет. Виновник — мой fix1: probe входа шёл
   на `/api/system`, а он **публичный** (без requireAdmin) — probe
   проходил с любым, даже мёртвым токеном, админка открывалась, и все
   профильные админские API отвечали 404.
3. Побочно подтверждено: RSSI заморожен на значении первого пакета
   (−89 дБм), `edges_dropped` растёт миллионами в минуту (шторм фронтов
   GDO0). Не критично; кандидат в тикет ядра — после W3.3 (там же
   появятся setFreqMHz/readRssiNow).

**Исправления (только wx.html + версия; ядро не тронуто):**
- probe входа и автовхода — на **админский** `/api/telemetry`
  (requireAdmin → 401 без валидного токена). Мёртвый токен теперь
  честно отбрасывается, админка призраком не открывается.
- `isAuthFail(r)`: 401 **или** 404 `unknown_dev_api` на админском пути =
  сессия мертва → автоматический «выйди-зайди» (sessionExpired с тостом),
  а не «Даталоггер недоступен».
- `fetch` с `cache:'no-store'` — API-ответы не кэшируются.
- Подсказка вкладки графиков: затирается при каждом заходе
  («Загрузка графика…»), сетевые исключения fetch выводятся в неё
  текстом (раньше — только мимолётный тост).

**Стенд (headless):** сценарий A (стухший токен при загрузке) — probe
401 → токен стёрт, админка не открылась; сценарий B (свежий вход) —
probe 200 → каналы → `/dlog?ch=0` ушёл → 5 чипов, подсказка чистая.
Ошибок JS нет.

**Ворота:** host 43751/0, ядерные 43351/0, сборка 4/4 (weather_gate
1078298 Б — diag-логи на месте, smart_lock 1093734, home_master 1095468,
test_rig 1057770).

**Урок №18 (diag2):** probe «живости токена» должен бить в эндпоинт,
реально проверяющий токен (requireAdmin), а не в публичный — иначе
проверка доказывает лишь «сервер жив». Сессии в RAM = после ЛЮБОГО
ребута токен мёртв; UI обязан трактовать 404 unknown_dev_api на
админском пути как истёкшую сессию (перелогин), потому что ядро
намеренно отвечает 404, не раскрывая существование путей.

---

## W3.2-diag3 — браузерная трассировка запросов (затык «сессия истекла»)

**Дата:** 26.08.2026. **Версия:** `0.3.2-diag3`. **Пакет:** `weather_gate_W3.2_diag3.zip`.

**Что показал diag2 (serial + аудит стенда):**
- Даталог окончательно здоров: 465+ пакетов подряд с `rc=1111`,
  unix валиден; «Тенденция давления: стабильно» на публичной панели —
  серверная статистика из того же даталога (читается!).
- Входы УСПЕШНЫ (аудит: 5×AUTH_LOGIN; лог: `dlog/channels: admin=ok`
  после каждого), но владелец получает «Сессия истекла» сразу после
  входа → какой-то запрос после enterAdmin возвращает 401/404, а
  по serial его не видно (ядреные эндпоинты не логируются, а
  `/api/dev/dlog?ch=…` до прошивки так ни разу и не дошёл — при восьми
  успешных `dlog/channels`!).
- Побочно замечено: OTA перезаписывает LittleFS целиком (fw+fs) —
  `no config file, defaults in use`, hostname сбросился на
  microos-bd612b. Конфиг переживает OTA только если fs-образ собран
  с ним. Отдельная тема, не трогаем сейчас.

**Что добавлено в diag3 (только wx.html + версия):**
- **Трассировщик запросов** в api(): каждый fetch — {время, метод, URL,
  HTTP-статус, миллисекунды, текст обрыва}, кольцо 16 записей, рендер
  во вкладке «Админ» → «Трассировка запросов». `/api/auth` пишется без
  query — пин в лог не попадает (секреты — табу).
- **AbortController, 10 с** на каждый запрос: «молчаливое зависание»
  (главный подозреваемый по невидимому `/dlog?ch=…`) превращается в
  запись трассировки «ОБРЫВ: таймаут 10 с — контроллер не ответил»
  с точным URL.

**Стенд (headless):** вход → «Админ»: трасса полная, `/api/auth` без
пина, ошибок JS нет. **Ворота:** host 43751/0, ядерные 43351/0, сборка
4/4 (weather_gate 1078298 Б).

**Снятие показаний владельцем:** прошить по OTA → войти → если снова
«Сессия истекла»: вкладка «Админ» (или открыть вход заново — трасса
жива в странице) → прислать скриншот/текст блока «Трассировка
запросов» + хвост serial. По паре «трасса браузера × serial
контроллера» затык локализуется однозначно: если запрос есть в трассе
с 200, но нет в serial — теряется в ядре; если в трассе ОБРЫВ — TCP;
если 401 — токен.

---

## W3.2-diag4 — трассировка до входа + мягкая смерть сессии

**Дата:** 26.08.2026. **Версия:** `0.3.2-diag4`. **Пакет:** `weather_gate_W3.2_diag4.zip`.

**Итог раунда diag3 (serial + аудит):** входы успешны (5×AUTH_LOGIN,
`channels: admin=ok` после каждого), `/dlog?ch=…` до прошивки так ни
разу и не дошёл, а «Сессия истекла» вылетает мгновенно — и закрывает
админку вместе с трассировкой (прибор оказался спрятан за дверью,
которую охраняет). Вывод: одиночный сбойный ответ после входа убивает
сессию по 401/404 без перепроверки.

**Изменения diag4 (только wx.html + версия):**
- Трассировка запросов **дублируется в модалке входа** — доступна по
  5 тапам БЕЗ входа в админку.
- **Мягкая проверка смерти сессии** (`isRealAuthFail`): одиночный
  401/404-unknown_dev_api больше не выкидывает из админки — страница
  перепроверяет токен probe'ом в `/api/telemetry`; probe жив →
  inline-ошибка в панели, сессия сохраняется; probe мёртв — честный
  «Сессия истекла».
- Таймаут 10 с на запрос и трасса (из diag3) на месте.

**Стенд:** симуляция бага (404 unknown_dev_api на /dlog при живом
токене) — админка НЕ закрывается, чипы каналов рендерятся, inline-текст
ошибки виден, JS-ошибок нет. **Ворота:** host 43751/0, ядерные 43351/0,
сборка 4/4.

**Владельцу:** прошить → 5 тапов → сразу видна трасса (без входа) →
ввести пароль → если графики снова не откроются, админка останется
открытой: прислать скриншот блока «Трассировка запросов» (модалка или
вкладка «Админ») — там будет точный виновник: URL, статус, время.

**Заметка в backlog (ядро):** в аудите попалась битая строка
(`"up":101727...` съехало в `"up6,`) — похоже на переполнение поля
upTime при записи; завести в Issue #1 при следующей партии.

---

### W3.2-diag5 — затык пойман: висячий pathTail в ядре (2026-08-26)

**Пара «трасса × serial» дала ответ.** Трасса diag4 (модалка входа, без логина):
`GET /api/dev/dlog?ch=0&range=6h → 404` за 33 мс с телом `unknown_dev_api` —
при живом токене (`/api/telemetry`, `dlog/channels`, `ota/info`, `config/backup`
в ту же секунду — 200). Тело `unknown_dev_api` ядро шлёт ТОЛЬКО когда профильный
`handleApi` вернул `false`. А в serial за все буты — полная тишина по `dlog`:
ни `admin=ok`, ни `admin=NO` (для `dlog/channels` строки есть). Обе ветки
нашего кода логируются — молчание значит: в `handleApi` пришёл `pathTail`,
который НЕ начинается с "dlog".

**Корень — в ядре, HttpService::handleApiDev:**
```cpp
const char* tail = _server.uri().c_str() + strlen("/api/dev/");  // String-временник!
bool handled = _ui->handleApi(tail, ...);                        // tail уже висячий
```
`WebServer::uri()` возвращает `String` по значению; временник умирает на `;`,
`tail` указывает в освобождённую кучу. Срабатывание — лотерея heap:
`/api/dev/dlog` (14 Б, ходовая 16-байтная корзина) затирается стабильно,
`/api/dev/lock/dlog` на замке (другая корзина) выживает — поэтому «на замке
графики замечательно отображаются», а у нас 404. Это же объясняет молчание
serial: содержимое буфера затёрто ДО strcmp.

**Правка профиля (diag5, один файл — WeatherGateApp.cpp):** безусловный лог
входа `api hit: tail='...' len=N tok=есть/НЕТ` первой строкой handleApi
(печатает tail дословно, %.40s) + лог `api miss` на финальном return false.
После прошивки serial покажет мусорный tail дословно — финальное
подтверждение для тикета ядру.

**Fix-заявка в Issue #1 (ядро):**
```cpp
String _uri = _server.uri();                       // держим живым
const char* tail = _uri.c_str() + strlen("/api/dev/");
bool handled = _ui->handleApi(tail, ...);
```
Проверить тот же паттерн в handleWebFile/handleNotFound.

**Попутно исправлено (diag4, подтверждено владельцем):** вход в админку больше
не «моргает» — мягкая перепроверка сессии (isRealAuthFail) работает, вход
проходит, ПАЗ/Система зелёные. «Сессия истекла» при успешном логине ушла.

**Ворота:** host-тесты профиля 43751 PASS/0 FAIL; ядро 43351/0; сборка
4/4 (weather_gate 1078474 Б). Пакет: weather_gate_W3.2_diag5.zip.

**После подтверждения мусорного tail:** ждать фикс ядра (5.8.3), затем
снять wx.diag (галочка) или решить об оставлении. Backlog ядра дополнен:
висячий tail (этот тикет, приоритет — ломает профильные API), битая строка
аудита, RSSI-заморозка, шторм edges_dropped.

---

### Ядро 5.8.3 — фикс pathTail принят (Issue #1, 26.08.2026)

**Ответ ядерной ветки:** баг подтверждён на 100% («маскировался годами за
длинными хвостами замка»), фикс принят в нашей формулировке (именованная копия
String на время handleApi), вошёл в **ядро 5.8.3**. Аудит остальных
`.c_str()` от временных String: чисто; `uiArgTrampoline` безопасен (копия
в кольцевой буфер при живом String). WxTrend.h — разделение слоёв отмечено
как образцовое.

**Приёмка на железе (критерий ядерной ветки):** после перешивки на 5.8.3
в serial `DIAG api hit: tail='dlog' len=4`, вкладка «Графики» отдаёт данные.
Владелец обновляется и запускает проверочный прогон.

**Ответные материалы (ответ_в_issue_1_gdo0_аудит.md):**
- шторм GDO0: замеры 115→230 тыс. фронтов/с между пакетами (pkt +1/48 с),
  гипотезы ядра поддержаны, стенд готов к экспериментальным билдам драйвера;
- RSSI-заморозка приложена к той же ветке (блокер для W3.3 readRssiNow);
- битая строка аудита: свежие выгрузки чистые, гипотеза — усечение буфера
  при `up` > 999999 мс (16,7 мин); сырые строки отправлены.

**Урок №19:** `String` по значению + `.c_str()` от временника = висячий
указатель. Читаемость бага — heap-лотерея по размерным корзинам: короткие
строки (16-байтная корзина) затираются первыми, длинные выживают и
маскируют дефект годами. Диагностический приём, решивший дело: лог
первой строкой обработчика + браузерная трасса запросов (пара «трасса ×
serial» делит вину между сторонами стека за один прогон).

**После приёмки 5.8.3:** решение по `wx.diag` (оставить/погасить) →
W3.3 (сканер АЧХ, дельта драйвера) → W5 (Замбретти + Open-Meteo, слот
WG_EVENT_STORM_WARNING зарезервирован ядром) → soak 72 ч по общему протоколу.

**Постскриптум по GDO0 (разбор IOCFG, 26.08.2026):** шторм — это шум сырого
демодулятора (IOCFG0=0x0D) между пакетами, ожидаемый by design; шторка GDO2
(CS) его и отсекает. Подтяжка 10к не нужна и не сработает (push-pull выход;
GPIO36 без внутренних подтяжек — но и окна риска нет: до init прерывание не
подключено). Реальная цена — ISR ~200 тыс/с впустую: просьба ядру —
маскировать прерывание GDO0 при закрытой шторке (их же гипотеза №3).
Метрика edges_dropped вводит в заблуждение — это rejected noise, не потери;
предложено переименовать. **Урок №20:** прежде чем чинить линию — читать
IOCFG: «шторм» оказался штатным поведением демодулятора, а пугающий счётчик —
не ошибкой, а штатным отвалом шума. Дополнение к ветке Issue #1 подготовлено
(ответ_в_issue_1_gdo0_аудит.md).

**ПРИЁМКА 5.8.3 ЗАКРЫТА (26.08.2026, ~20:00):** serial показывает
`api hit: tail='dlog' len=4 tok=есть` — фикс ядра работает на железе.
`dlog?ch=0/1/2 → 200` (45–90 мс), графики рисуются (температура, давление),
raw-точки читаются (n=4..5, dropped=0). Баг W3.2 «Нет данных (HTTP 404)»
закрыт окончательно: это был висячий pathTail ядра, с первого дня.
Ядерной ветке отправить подтверждение приёмки (tail='dlog' len=4 — их
критерий). Открыто: решение по wx.diag (оставить/погасить — поле в /admin),
RSSI-заморозка (к W3.3), маскирование ISR GDO0 (запрошено у ядра).

---

### weather_gate 0.3.3 на ядре 5.8.4 — поле wx.rf_agcctrl1 (2026-08-26)

**Ядро 5.8.4 опубликовано и сверено по коду (GitHub main):**
1. pathTail — именованная копия String (HttpService.cpp:708-710) ✅
2. Маскирование ISR GDO0 через GPIO.pin[].int_ena (регистровый уровень,
   IDF-API не в IRAM) ✅
3. wx.rf_agcctrl1 — драйвер пишет регистр 0x1C при значении 1..255
   (cfgGetInt, writeRxTable) ✅
4. setFreqMHz/readRssiNow в драйвере (task-контекст) — W3.3 разблокирован ✅
5. repairFileTail в AuditService — обрезка битого хвоста при загрузке (5.8.4) ✅

**Поправка ядра по семантике edges_dropped ПРИНЯТА:** счётчик — это
переполнения кольца при ОТКРЫТОЙ шторке (CS с reset-дефолтом AGCCTRL1
открывается на собственный шум), а не шум закрытой шторки. Метрику не
переименовываем — после маскирования это честный счётчик, норма ~0.

**Правка профиля (один файл, WeatherGateApp.cpp):** поле wx.rf_agcctrl1
(ConfigType::INT, 0..255, дефолт 0, CFG_CRITICAL) добавлено В КОНЕЦ схемы,
группа «Телеметрия и сторожа». База для подбора — 0x40 (бит 6 =
AGC_LNA_PRIORITY, как в reset). Кодировку порогов подбираем на стенде
по даташиту/SmartRF — в ядро не зашита, правильно.

**Ворота на 5.8.4:** host профиля 43751/0, ядро 43351/0, сборка 4/4
(weather_gate 1080202 Б, test_rig 1059078, smart_lock 1094590,
home_master 1096752). Пакет: weather_gate_W3.2_5.8.4_agc.zip.

**Хозяйственное:** каноничное дерево mos_main пересобрано с GitHub main
(5.8.4). ВНИМАНИЕ: в репозитории имена кириллических docs хранятся
в экранированном виде (#U0418...) — при выгрузке на /mnt они застревают
(неудаляемый фантом в mos_main_old, удалить не удалось). Кодовые файлы
все ASCII — на сборки не влияет. Старое дерево: /mnt/agents/work/mos_main_old
(мусорный остаток, не использовать).

**Бенч 5.8.4-pre (у владельца):** критерии — edges_dropped ~0 при живых
пакетах (pkt +1/48 с), затем подбор wx.rf_agcctrl1 до чистого 0 И
устойчивого приёма. Стартовая точка: 0x40 (только бит 6), далее
относительный порог CS по даташиту (биты 4-5: MAGN_TARGET, биты 0-3:
пороги). Результаты — в Issue #1, дальше 5.8.4 на флот.

**RSSI-проверка (п.2 ядра):** readRssiNow при шевелении антенны на стенде —
живой → вопрос закрыт; заморожен → копаем AGC вместе. Ждём билд 5.8.4-pre
у владельца (метод уже в драйвере, вызов — из отладочного API или W3.3).

**Бенч wx.rf_agcctrl1 (26.08.2026, вечер):** лесенка относительного порога
CS 0x40/0x50/0x60/0x70 — НУЛЕВОЙ эффект (edges 127–145 тыс/с на всех
шагах, pkt +12 везде). Поле до регистра доезжает (порядок фаз ядра верен).
Вывод: относительный порог бессилен при AGC на макс. усилении в шуме
(оценка шума утекает с усилением). Следующий рычаг — абсолютный порог
(биты 3:0, лесенка 0x41–0x4F); карту шагов подтвердить у ядра (AGCCTRL2
в RX-таблице не задаётся, MAGN_TARGET = reset). Побочка: RSSI заморожен
ВНУТРИ бута (−90 по всему serial), но различается МЕЖДУ бутами
(−87/−95/−97/−90) — похоже на защёлку при init AGC, а не на «честную
стабильность». Дубли 33/13 — шторка дребезжит на шуме.

**Длинный прогон 8ч18м (27.08.2026):** pkt=502 (+1/60 с), dup/pkt≈2,4,
edges 3,42e9 (~114,6 тыс/с ровно). Система стабильна. **RSSI-баг
доказан экспериментом:** антенна двигалась внутри шага — RSSI не менялся;
изменился только после ребута. Латч читается/обновляется один раз при
init AGC, за аптайм не обновляется. Тема для ядра: readRssiNow наружу
(бенч-билд) + чтение «RSSI последнего пакета» перенести в момент приёма.

---

### weather_gate 0.3.4 на 5.8.4-pre3 — probe RSSI, вторая ступень AGC (27.08.2026)

**Ответ ядра (27.08) — ключевое:**
- СТОП на нашу лесенку 0x48–0x4F: ABS_THR — допкод! 0x41..0x47 = +1..+7 дБ,
  **0x48 = абсолютный CS ОТКЛЮЧЁН (ловушка)**, 0x49..0x4F = −7..−1 дБ.
  Правильная лесенка ВВЕРХ: 65–71 dec. Урок №22: битовые поля регистров
  RF-чипов кодировать ТОЛЬКО по таблице даташита, «монотонность» нельзя
  предполагать.
- База: AGCCTRL2 reset 0x07 → MAGN_TARGET=42 дБ → порог CS ≈ −90,5 дБм,
  наш сигнал −88..−97 сидит НА пороге → ждём стоп pkt уже на +1..+3 дБ.
- RSSI-заморозка РАЗГАДАНА (даташит 17.3): RSSI читается непрерывно ДО
  детекта sync, после — заморожен ДО СЛЕДУЮЩЕГО ВХОДА В RX. Мы входили в
  RX раз на бут — отсюда латч на бут и скачки между бутами. Наш антенный
  эксперимент ядро признало решающим.
- pre3: драйвер САМ перевходит в RX (SIDLE→SFRX→SRX) через 2,5 с после
  пакета + сторожевые 10 мин (перекалибровка AGC против дрейфа кварцев);
  слепота ~1 мс на 48–60 с. Геттер rxReenters() — контроль механизма
  (норма ~1/пакет + ~6/час).

**Правки профиля 0.3.4:**
1. wx.rf_agcctrl2 (INT 0..255, 0=не писать, CFG_CRITICAL) — В КОНЕЦ схемы
   рядом с agcctrl1. Вторая ступень: MAX_LNA_GAIN вниз, если +7 дБ ABS_THR
   не хватит.
2. /api/dev/radio: поле rx_reenters всегда; probe-контракт —
   ?probe=1 → rssi_now (readRssiNow); ?probe=2 → setFreqMHz(текущая)
   (ручной перевход RX) + rssi_now. Публичный (RSSI — не секрет), SPI
   только из task-контекста, как предписано.

**Ворота:** host 43751/0, ядро 43351/0, сборка 4/4 (weather_gate 1080838 Б).
Пакет: weather_gate_0.3.4_5.8.4pre3.zip. Дерево mos_main пересобрано с
GitHub main (pre3), профиль 0.3.3 восстановлен из нашего пакета (GitHub
несёт diag5 — ядро держит у себя старый слепок профиля, учитывать при
синках!).

**Бенч pre3 (у владельца, билд weather_gate_5.8.4-pre3.bin):**
1. Прошить pre3 + профиль 0.3.4, wx.rf_agcctrl1=0.
2. Контроль механики: GET /api/dev/radio → rx_reenters растёт (~1/пакет);
   rssi (последнего пакета) МЕНЯЕТСЯ между пакетами; ?probe=1 → rssi_now
   живой (шевелить антенну).
3. Лесенка ABS_THR ВВЕРХ: 65 (0x41) → 66 → 67… стоп на первом пропадании
   pkt, полшага назад. Критерии: edges≈0, pkt +1/48–60 с, dup/pkt→~1,0.
4. Если +7 (71) не душит шторм — вторая ступень wx.rf_agcctrl2 (карту
   MAX_LNA_GAIN попросить у ядра по SmartRF).
5. Сравнить загрузку CPU с 5.8.3 (маскирование ISR) — по /api/telemetry.

**Soak 1д18ч на 5.8.4-pre (27–28.08.2026):** pkt=2578 (+1/59 с), dup/pkt≈2,25,
ПАЗ зелёный, графики 24ч с ярусами — красота. АНОМАЛИЯ: edges_dropped=18,95M
после 3,42e9 на прошлом прогоне → счётчик uint32 ПЕРЕПОЛНЯЕТСЯ каждые ~10,4 ч
при 114 тыс/с; абсолютам на длинных окнах верить нельзя, только приращениям.
Ядру: uint64 или reset-on-read. Владелец уходит на бенч pre3+0.3.4
(протокол: этап 0 — механика RSSI/rx_reenters/probe; этап 1 — лесенка
65–71 до стопа pkt; этап 2 — agcctrl2 только по карте ядра).

**Бенч pre3, этап 0 — ЗЕЛЁНЫЙ (28.08.2026):** RSSI ожил (−89…−95 в serial
от пакета к пакету), rx_reenters≈pkt (14/14), probe=1 отдаёт живой
rssi_now (−94 при латче −87). RSSI-тема закрыта. База шторма: ~135 тыс/с.
**Ошибка лесенки:** значения 65–68 введены в wx.rf_agcctrl2 вместо
wx.rf_agcctrl1 (поля соседние). Побочный полезный факт: MAGN_TARGET 33 дБ
(agcctrl2=65..68) — приём устойчив, запас по усилению есть. Лесенку
ABS_THR повторить в agcctrl1. Наблюдение владельца: dup/pkt мал в начале
шага и растёт к концу — AGC «разгоняется» в шум после ребута; аргумент
за абсолютный порог.

**Бенч pre3, лесенка ABS_THR в правильном поле (wx.rf_agcctrl1=65–70,
29.08.2026) — ЭФФЕКТА НЕТ.** Serial подтверждает: значение в 0x1C,
ребут на каждом шаге, байт применялся. Темпы шторма 118–183 тыс/с — в
пределах шума базы; приём цел везде (+13…+14 pkt/шаг), включая пакеты с
RSSI −94 дБм при пороге, который обязан был срезать всё ниже −85,5 дБм
(шаг 70 = +6 дБ). Вывод: не «децибел мало», а CS не реагирует на запись
0x1C в нашем режиме — качественный вопрос ядру (перезапись при активном
RX? особенности ABS_THR в async?). Просадка на шаге 69 (pkt 7→8, разрыв
385 с, «возраст 6 мин») — НЕ стоп: приём сам восстановился; такие же
провалы 144–288 с на шагах 68/70 — плавающие пропуски слабых пакетов.
ABS-ступень исчерпана. Запрос ядру: карта кодов AGCCTRL2 (MAX_LNA_GAIN
вниз) + консультация по применению 0x1C + контрольное чтение регистра
обратно. Заметки: edges_dropped uint32 переполняется (~10,4 ч) — uint64/
reset-on-read; на GitHub профиль — слепок diag5, синхронизировать.
Шаг 71 (0x47) отложен: ожидание нулевое, добьём по запросу ядра.
Шаг 71 (0x47, +7 дБ — максимум ABS) добит: pkt=13, dup=28, edges≈86,25M
(~135 тыс/с), RSSI −88, приём живой. Лесенка 0x41–0x47 закрыта ПОЛНОСТЬЮ,
эффекта нет на всём диапазоне. Запрос ядру дополнен точкой 71 и отправлен
на размещение владельцем.

**Бенч pre4 + профиль 0.3.5, probe=3 — ПЕРЕЛОМ (29.08.2026):** readback
readConfigReg показал: при wx.rf_agcctrl1=71 в конфиге чип держит 0x1C=0x40
(reset-дефолт) — **запись ABS_THR в чип не применяется**. Вся лесенка 65–71
была прогоном при дефолтном регистре; «отсутствие реакции CS» объяснено,
кремний ни при чём. Урок №23: подбор параметра без readback = гадание;
любой бенч регистров начинать с контрольного чтения. Второе: чип живёт на
AGCCTRL2=0x03 (MAGN_TARGET=33 дБ), а не 0x07/42 дБ, как считало ядро —
база порога ещё на ~8 дБ ниже. Корреляционный замер 60 с при дефолте:
шторм 117 тыс/с при полке rssi_now −89 дБм > табличного порога −90,5 —
CS честно открыт, аномалии нет. rssi_now заморожен между reenter
(латч обновляется по перевходам RX). Ждём фикс записи от ядра, затем
лесенка 65–71 повторно.
A/B-прогон владельца (0↔71 со скриншотами и ребутами) подтвердил: при 71
в конфиге чип читает 0x40 в обоих состояниях. Невнимательность исключена,
вердикт «запись не применяется» — окончательный. Ответ ядру обновлён.

**pre5 (29.08.2026): корень «записи в никуда» найден ядром — ПОРЯДОК
ЗАГРУЗКИ.** DriverRegistry::init (→ writeRxTable → cfgGetInt) выполнялся
ДО ConfigService::init (loadFromJson) — драйвер видел дефолт схемы «0»,
регистр не писался. Наш кандидат №1 подтверждён. Латентная бомба для
всей флотилии (wx.rf_freq_mhz игнорировалась бы так же). Фикс: ConfigService
поднят выше DriverRegistry. pre5 = pre4 + порядок загрузки + DIAG-строка
"[cc1101] cfg: agcctrl1=N agcctrl2=N" + readMarcstate() + комментарий
AGCCTRL2 (reset 0x03 подтверждён ядром, его 0x07 была ошибкой; MAGN_TARGET
= 33 дБ, база порога ещё на ~9 дБ ниже). Профиль 0.3.6: probe=3 дополнен
marcstate (ожидание в RX: 0x0D). Ворота: профиль 43751/0, ядро 43351/0,
сборка 4/4 (weather_gate 1081338 Б). Приёмка: при 71 в конфиге serial
показывает agcctrl1=71, probe=3 читает 0x47 — затем настоящая лесенка.

**pre5 + профиль 0.3.6 — фикс записи ПОДТВЕРЖДЁН (29.08.2026).** Serial:
"[cc1101] cfg: agcctrl1=71" — конфиг виден драйверу; probe=3: agcctrl1=0x47,
agcctrl2=0x03, marcstate=0x0D. Настоящая ABS-лесенка 65–71 (каждый шаг
подтверждён readback): шторм 108–154 тыс/с на всех шагах — подавления НЕТ,
приём цел (+12 pkt/шаг). Честный ноль: база 33 дБ → порог ~−99 дБм, +7 дБ →
~−92, а полка шума −89 дБм выше — до отсечения не хватает ~3+ дБ сверх
ABS-максимума. dup/pkt на шагах 67–71 спадает до 1,15–1,9 (против 2,8) —
порог «щекочет» шторку. Следующее: вторая ступень MAX_LNA_GAIN (agcctrl2,
лесенка 11→19→27→35 при agcctrl1=71) — ждём карту «ступень→дБ» от ядра.

**Ядро дало карту MAX_LNA_GAIN (29.08.2026):** ступени −2,6/−6,1/−7,4/
−9,2/−11,5/−14,6/−17,1 дБ; ABS+LNA складываются, agcctrl1=71 держать.
Модель порога: −92 → шаг1 −89,4 → шаг2 −85,9 → шаг3 −84,6 → шаг4 −82,8;
полка −89 — пересечение на шаге 1–2. Ядро честно предупредило: RSSI пакетов
−81…−94 перекрывается с полкой, просадка pkt ожидается рядом с падением
шторма — границу измеряем. Критерий ступени: pkt +12 обязателен. Запасной
рычаг: MAGN_TARGET 33→42 (agcctrl2=7) поднимает весь стол ~9 дБ БЕЗ срезания
усиления (в эфире блокеров нет). Программа: 1) лесенка 11/19/27/35;
2) если LNA съел pkt — agcctrl2=7 + повтор ABS 65–71; 3) комбо 0x07+ABS max.
Принцип закреплён ядром: таблица из головы требует readback или цитаты.

**Вторая ступень (MAX_LNA_GAIN, agcctrl1=71) — ШТОРМ УБИТ (29.08.2026).**
Шаг 1 (0x0B, −2,6 дБ): темп упал ~130→25 тыс/с. Шаг 2 (0x13, −6,1):
шторм убит (65/с след). Шаги 3–4 (0x1B/0x23): edges=0, приём ПОЛНЫЙ
(+12 pkt на всех шагах!) — граница pkt ещё не найдена, ядро ждало
просадку на шаге 2, ошиблось в нашу пользу. dup/pkt стал РОВНО 5,0 на
всех тихих шагах: уличный блок шлёт серию из 6 идентичных копий, раньше
шторм резал серию хаотично (2,2–2,4 «плавающие») — дедуп оказался
термометром шторма. План: добить лесенку 43/51/59 до границы pkt, откат
на 1–2 ступени, soak 1 ч, рабочая точка — кандидат 27 (0x1B) или 35.
Отчёт ядру с открытым финалом отправлен (владелец шагает 43/51/59).
Заметка: норма dup/pkt в наших ранних записках «≈1,0» — неверна, здоровая
серия станции это 5,0 (6 копий); предложено ядру сверить с rtl_433 и
зафиксировать как норму ПАЗ.

**Лесенка второй ступени ЗАКРЫТА (шаги 5–7, 29.08.2026):** 43/51/59 —
edges=0, pkt +12/+14 везде, граница pkt НЕ найдена до упора шкалы (−17,1 дБ).
Serial-ведомость полная (cfg: agcctrl2=11→59 на всех ребутах). dup/pkt = 5,0
стабильно. РАБОЧАЯ ТОЧКА: wx.rf_agcctrl1=71 (0x47) + wx.rf_agcctrl2=27 (0x1B):
шторм умирает на шаге 2, берём +1 ступень запаса, 4 ступени усиления до конца
шкалы сохранены. Далее: soak 1 ч на рабочей точке → кит 5.8.4 на флот → бенч
зеркала W4 → UI на edgesDroppedTotal. Ядру предложено: сменить дефолт
writeRxTable (0x47/0x1B) или профиль выставит свои дефолты схемы.
Замечание владельцу: «улучшение RSSI» на шаге 7 (−83…−87) — скорее честнее
измерение (AGC не разгоняется на шуме), чем сильнее сигнал; кросс-шагово
RSSI не сопоставим.

**Soak 16ч13м на рабочей точке (0x47/0x1B) — ЗЕЛЁНЫЙ (30.08.2026):**
pkt=1133 (+1/51,5с), dup/pkt=4,99 (зеркало серии 6 копий — статистически
стойкая норма), edges 8,61M/16ч ≈ 148/с (след, в ~770 раз ниже шторма),
ПАЗ здорова (8/0 — состав при случае), ребутов нет. **Тема шторма GDO0 /
Carrier Sense ЗАКРЫТА.** Итог эпопеи: readback (урок №23) → порядок
загрузки (pre5) → честный ноль ABS → вторая ступень убила шторм на −6,1 дБ,
приём полный до −17,1 дБ. Следующие шаги: кит 5.8.4 на флот, бенч зеркала
W4 на мастере, UI на edgesDroppedTotal, решение по wx.diag, W3.3/W5.

**Профиль 0.3.7 — дефолты схемы AGC по запросу ядра (30.08.2026):**
Ядро дало добро на флот-кит 5.8.4 и передало запрос: «ядро дефолт чипа
не трогает, профиль 0.3.7 несёт 71/27 дефолтами своей схемы».
Правка ровно одна: дефолты полей wx.rf_agcctrl1 «0»→«71» (0x47, макс.
ABS-порог) и wx.rf_agcctrl2 «0»→«27» (0x1B, LNA −7,4 дБ); комментарии
полей обновлены по итогам бенча 29.08 (reset чипа 0x03, рабочая точка 27).
Семантика «0=не писать» сохранена. Ворота качества: host-сьют профиля
43751 PASS/0 FAIL, ядра 43351 PASS/0 FAIL; сборка 4 профилей зелёная,
отпечатки сошлись 1:1 (weather_gate 1081338, smart_lock 1094590,
test_rig 1059078, home_master 1096752 Б). ВНИМАНИЕ: дефолты схемы влияют
только на СВЕЖИЕ установки — на железе владельца 71/27 уже живут в NVS.
Контроль после прошивки: serial-строка «[cc1101] cfg: agcctrl1=71
agcctrl2=27» и probe=3 → 0x47/0x1B. Далее: флот-кит 5.8.4 (замок и
мастер — у владельца, шлюз — финальной сборкой), бенч зеркала W4,
UI на edgesDroppedTotal (разблокировано).

**Флот-кит 5.8.4 прошит на всех трёх устройствах (30.08.2026, ~19:50):**
порядок шлюз → замок → мастер, все показывают «5.8.4 · сборка ee57070»,
OTA-истории чистые (прошивка + ФС, без откатов), NVS-бэкапы сделаны.
Шлюз: приём жив (RSSI −87 дБм, последний пакет 3 с назад, батарея норма).
Мастер: брокер и мост М2 на связи. Норма ПАЗ dup/pkt подтверждена ядром.

**Приёмка 0.3.7 на железе — ЗЕЛЁНАЯ (30.08.2026, ~20:10):**
probe=3 → agcctrl1=0x47, agcctrl2=0x1B, marcstate=0x0D (RX),
dup/pkt = 70/14 = ровно 5,0 (норма подтверждена ядром), healthy=1.
Флот: замок 5.8.2→5.8.4 по журналу парка, мастер — брокер :1883 (2/12),
мост М2 108↑/2↓ без потерь. Мелочь на заметку: ПАЗ мастера 1 warn/0 crit
(hm.sd ok) — посмотреть состав при случае. Флот-кит 5.8.4 закрыт.
Далее: бенч зеркала W4 на мастере → soak 72 ч → тег v5.8.4.

**Профиль 0.3.8 — MQTT: discovery, зеркало W4, учёт публикации (30.08.2026):**
Запрос ядра (Issue): шлюз не публикует HA discovery и weather-JSON,
нет wx.mirror_topic. Расследование: discovery в репозитории НЕ существовало
никогда (проверены все коммиты профиля — ранняя видимость в HA была от
старой монолитной прошивки); путь публикации weather-JSON статически
корректен, но publishRaw мог молча вернуть false — ненаблюдаемо.
Правки: publishHaDiscovery() на SH_EVENT_MQTT_CONNECTED (14 сущностей:
temp/feels/hum/wind/gust/dir/rain/press/rssi/batt/age из weather-JSON +
uptime/heap/cpu_t из telemetry; образец smart_lock, availability на LWT);
поле wx.mirror_topic (STRING, пусто=выкл, CFG_NONE — без ребута);
учёт rc публикации с логом по фронту + свежая retained-погода на каждой
MQTT-сессии. Бюджет weather-JSON проверен: макс ~206 Б < 256.
Ворота: 43751/0 + 43351/0; сборка 4 профилей зелёная (шлюз 1085702 Б,
остальные без изменений).

**Профиль 0.3.9 — каноничные единицы HA в discovery (30.08.2026):**
Прогон 0.3.8 на стенде: discovery доехал (retained 16→30, ровно +14),
устройство weather_gate появилось в HA — но с 8 объектами из 14. Точное
совпадение: HA отбросил 6 конфигов с локализованными единицами
(«м/с», «мм/ч», «гПа», «дБм», «с») — валидация пары device_class +
unit_of_meas. Заменено на каноничные m/s, mm/h, hPa, dBm, s.
Отдельная аномалия: weather-JSON на брокере так и не осел (retained 30,
а не 31) — несмотря на то, что discovery тем же publishRaw прошёл.
Локализация по журналу шлюза (строки «mqtt: weather pub» — учёт rc,
введённый в 0.3.8). Ворота: 43751/0 + 43351/0; сборка 4 профилей
зелёная (шлюз 1085686 Б, остальные без изменений).

**Приёмка 0.3.9 — ЗЕЛЁНАЯ (31.08.2026, ~00:45):** все 14 объектов в HA
живые (улица 26,5 °C/32 %, давление 1006,76 гПа у.м., RSSI −88 дБм,
батарея норма, возраст пакета, ветер/дождь/направление + аптайм/heap/
CPU). Мягкий ребут шлюза восстановил availability (online последним).
**Найден дефект моста М2 (сторона home_master/ядра):** synth offline по
RemoveClient не имеет стража «клиент переподключился» — при жёстком
обрыве сессии synth offline приходит ПОСЛЕ online новой сессии и
навсегда затирает retained state на верхнем брокере → объекты HA
«Недоступно» при живом устройстве. Доказательства: прослушка retained
(state=offline при живом потоке), журнал парка (client_left 00:06:11 →
online 00:12:38, synth 6→8), лог шлюза (одна чистая сессия). Профильная
часть запроса ядра закрыта: discovery (14), weather-JSON, wx.mirror_topic.

**Гонка synth offline — закрыта ядром (31.08.2026):** home_master
0.5.1-m3 (BrokerService 0.1.6-m3: hasClient по таблице принятых сессий;
BridgeService 0.1.3-m2: страж перед synth offline, журнальная строка
«synth offline пропущен — уже переподключился»). Ядро не менялось
(BIN_TAG MICROOS|5.8.4|END). Мастер прошит хотфиксом (OTA, только .bin).
Приёмка гонки: жёсткий обрыв питания шлюза → возврат; ожидание —
строка «пропущен» в журнале мастера, объекты HA доступны, retained
state=online. Контроль нормы: мягкий ребут — synth штатно, затем online.
После зелёной гонки: soak 72 ч → тег v5.8.4.

**Приёмка гонки synth offline — ЗЕЛЁНАЯ (31.08.2026, ~08:50):**
жёсткий обрыв связи шлюз-мастер (08:43:43 client_left) — synth offline
пропущен стражем, retained state=online не застыл (прослушка 8:42/8:45),
объекты HA доступны; возврат связи 08:48:20 — данные текут. Мягкий ребут —
штатный порядок offline→online. Замок вернулся в сеть штатно. Мост М2
(0.5.1-m3) закрыт. Линия MQTT/discovery закрыта ЦЕЛИКОМ.
Далее: soak 72 ч флота → тег v5.8.4.

**Soak 72ч — сутки 1/3 ЗЕЛЁНЫЕ (01.09.2026, 08:30):**
шлюз аптайм 23ч45м, pkt 1658, dup/pkt 4,98, edges ~210/с; мост
14982↑/96↓, потери 0, synth 3 (объяснимые); замок аптайм 1д, управляется.
Флажки наблюдения: ПАЗ мастера 200 warn (был 1 — множится, снять состав),
ПАЗ шлюза 247 warn (то же), heap-минимумы замка дрейфуют вниз
19560→17956 Б за ~36 ч (свободно 36 КБ; не тревога, но следить —
при продолжении уйдёт ядру как подозрение на утечку).

**Уточнение к суткам 1/3 (01.09.2026):** warn'ы ПАЗ мастера (200) и
шлюза (247) идентифицированы — NTP-ретраи (1–3 попытки синхронизации,
видны в мониторе порта), фон, не дефект. Ядерная ветка предупреждена
владельцем. В счётчиках soak'а NTP-warn читаем как фон. Heap замка —
на контроле (критерий: дно минимумов к финалу 72 ч).

**Soak 72ч — сутки 2/3 ЗЕЛЁНЫЕ (02.09.2026, 08:30):**
шлюз аптайм 1д23ч, pkt 3325 (+1667/сутки ~1/52с), dup/pkt 4,97,
edges ~268/с; мост 29847↑/192↓, потери 0, synth 3 (не вырос);
NTP-warn мастера +7/сутки (темп честный, фон); замок аптайм 2д,
14 циклов. **Heap замка: дрейф остановился** — новых минимумов за
сутки нет (дно 17956 Б от 01.09), цепочка была затухающей, не утечкой;
снято с контроля. HA: все три устройства живые, аптайм-сенсоры
сходятся с реальностью. Флот зелёный.

**Soak 72ч — сутки 3/3 ЗЕЛЁНЫЕ, ФИНАЛ (03.09.2026, 08:35):**
шлюз аптайм 2д23ч (HA 258616 с), ПАЗ ЗДОРОВА (186/0), сторож взведён,
wg.radio/wg.bme280 OK, пакет 11–33 с, RSSI −86…−91 дБм, heap 66 КБ.
Мастер 0.5.1-m3, аптайм 3д9ч: брокер rx 44828, отказов 0, retained 30,
клиенты 2/12; мост 44789↑/260↓, **потери 0**, **synth offline 3 — не вырос
за третьи сутки**; SD смонтирована; журнал парка — только штатные циклы
замка и web-логины. Замок аптайм 3д04ч (HA 260432 с), 23 цикла, MQTT
онлайн, ПАЗ активна, тревог нет; heap-минимум последний 17956 Б от 01.09 —
новых минимумов 48+ ч, дрейф затухший, не утечка. HA: все три устройства
живые, команды проходят, аптайм-сенсоры сходятся. Аптаймы мастера и замка
> 3 суток подтверждают: ни одного скрытого ребута за soak.
**Флот прошёл soak 72 ч — ЗЕЛЁНЫЙ. Готов к тегу v5.8.4.**
Ядру уходит сводный отчёт soak'а (три суточных среза + итоги по линиям
CS-шторм, MQTT/discovery, мост).

**Ответ ядра по soak (03.09.2026):** soak ПРИНЯТ, тег v5.8.4 согласован,
ставится после сборки кита. Состав кита: weather_gate 0.3.9, test_rig 0.1.0
(бинарники — от профильной ветки), home_master 0.5.1-m3 и **smart_lock
5.5.14** (не 5.5.13: правило 23 — порядок вкладок/закрытая зона; правило
зеркала — веб-доступ теперь подчиняется blocked/expiry, 403; авто-чистка
просроченных ключей lock.purge_expired; приёмка 7/7, 1095530 Б).
Issue #1 закрывается при публикации Release.

**Урок №24: отпечаток из памяти без readback — всё равно гадание.**
В сводном отчёте указал «smart_lock 5.5.13 — 1094590 Б» по памяти:
5.5.13 был 1093730 Б, а 1094590 Б — промежуточная нерелизная сборка.
Ядро поймало. Правило «таблицы из памяти требуют readback» распространяется
и на отпечатки чужих профилей: цитировать только из readback/паспорта ядра.

**Очередь после тега (по ядру):** smart_counter C1 (старт), ревизия
закрытых зон UI weather_gate и home_master по правилу 23 (эталон —
smart_lock 5.5.14), W4 (зеркало wx_* на мастере), W5 (прогноз), P4-миграция.

**Бинарники для кита v5.8.4 переданы (03.09.2026):** пересборка из
каноничного дерева (GitHub main + профиль, ядро esp32:esp32@3.3.11).
Ворота: host-сьют 43751 PASS / 0 FAIL. Отпечатки compile — точное
совпадение с запросом ядра: weather_gate 0.3.9 = 1085686 Б,
test_rig 0.1.0 = 1059078 Б. Файлы .bin (с образным заголовком):
weather_gate 1085840 Б, md5 bb15bc6e17cd8aa7295175367b28d350;
test_rig 1059232 Б, md5 3b7bbe48d18781c9b0c4cb063ee6f2a7.
Ждём тег v5.8.4 и Release.

**Скорректированный ответ ядра (03.09.2026, после синхронизации
GitHub):** тег v5.8.4 ставится **на HEAD main**, а не на e77f8b14 —
smart_lock 5.5.14 синхронизирован в main после нашего среза, иначе тег
зафиксировал бы дерево с дефектом зеркала. Логично, принято.
**Каноничное дерево синхронизировано: e77f8b14 → HEAD 5dcaf5cf**
(4 коммита: home_master 0.5.1-m3, smart_lock 5.5.14, weather_gate 0.3.9
теперь официально в main, доки). Локальные артефакты вычищены (обрезанные
имена доков, docs-как-файл, пустой MicroOS/projects). Сверка diff -rq:
дерево = HEAD main, кроме build-артефактов и дорожной карты (наша впереди,
append-only). Компиляционный прогон всех 4 профилей на новом дереве:
weather_gate 1085686 Б (совпал), test_rig 1059078 Б (совпал),
smart_lock 1095530 Б (**точное совпадение с отпечатком ядра 5.5.14** —
независимое воспроизведение), home_master 0.5.1-m3 1096960 Б (новый
отпечаток). Переданные бинарники валидны и на новом дереве.
Ждём тег v5.8.4 на HEAD main и Release.

**РЕЛИЗ v5.8.4 ВЫПУЩЕН (03.09.2026):** тег на HEAD main, Issue #1 закрыт
как completed — линия MQTT/discovery/мост завершена целиком. Релиз-нота
подтверждает состав флота: smart_lock 5.5.14 · weather_gate 0.3.9 ·
home_master 0.5.1-m3 · test_rig 0.1.0. Кит «МикроОС_5.8.4_флот_кит.zip»
в Assets: прошивки (5 файлов, md5 в ПАСПОРТе), полное дерево (= HEAD main),
ПАСПОРТ с верификацией и дозорами. Наши метрики soak вошли в релиз-ноту
(мост 44789↑/260↓ потери 0, synth 3→3, отказов брокера 0, скрытых ребутов
нет). Известные дозоры релиза: NTP-ретраи (фон), heap замка min 17040 Б,
CPU замка 76–78°C (порог поднят до 77°C), дубль ModuleId 0x1104 (после
тега). Очередь после тега: smart_counter C1 → ревизия UI шлюза/мастера
по правилу 23 → W4/W5 → P4-миграция.
**Линия «5.8.4 → релиз» закрыта. CS-шторм, MQTT/discovery, мост М2 —
все линии сезона завершены зелёными.**

**0.3.10 — правило 23 для UI шлюза (03.09.2026):** ревизия закрытой зоны
по эталону smart_lock 5.5.14. Аудит: конфиг-группы wx.html не рендерит
(вкладка «Админ» — ссылка на ядерную /admin), поэтому часть «Служебные
не отображаются» к шлюзу неприменима; предмет ревизии — порядок вкладок.
Было: Графики, Радио, Сеть, ПАЗ, Система, Админ (профильные первыми —
нарушение). Стало: **Сеть, ПАЗ, Система, Админ → Графики, Радио**,
вход в настройки — с вкладки «Сеть» (showTab('net')), подсказка в модале
входа приведена к тому же порядку. Правки: wx.html (4 точки одной
логической правкой) + версия в WeatherGateApp.h. Ядро не тронуто.
Ворота: host 43751 PASS / 0 FAIL; компиляционный прогон 4 профилей —
weather_gate 1085686 Б, test_rig 1059078 Б, smart_lock 1095530 Б,
home_master 1096960 Б. ФС: таблица разделов прочитана из сборки
(gen_esp32part, default 4MB: spiffs @0x290000, 1408K) — никаких гаданий
по размеру образа; mklittlefs -p 256 -b 4096 -s 1441792, стейджинг+chmod,
round-trip распаковкой 3/3 MATCH (коду возврата не верим — урок кондиций).
version.json: fw_md5 bd79f0abf250119eb7c2e0f7ba1f701f,
fs_md5 fc9743e0138aa9d6172250cb3c02e32a. Выдача: пакет профиля
weather_gate_0.3.10_5.8.4.zip + app-бинарь + littlefs.bin.
Приёмка на железе — за владельцем: порядок вкладок после входа,
все 6 вкладок на месте, контент переключается.
Далее: W3.3 — сканер лучшей частоты приёма (по максимуму уровня сигнала).

**0.4.0 — настроечные панели закрытой зоны (03.09.2026, запрос владельца):**
«не всегда удобно для настроек погружаться в ядерную /admin». Перенесён
движок панелей smart_lock 5.5.14 (источник истины GET /api/config —
профильно-нейтрален; запись поштучно только изменённых, dirty-tracking;
секрет пустым не отправляется; DHCP прячет статические поля; явный порядок
полей для «Сеть»/«Система»). Состав вкладок (whitelist, чужого нет):
- **Сеть**: статус + группы «Сеть» и «MQTT» (с пометкой о ребуте);
- **Радио**: статус приёмника + профильные группы «Радио CC1101»,
  «Датчик BME280», «Телеметрия и сторожа»;
- **Админ**: смена пароля (/api/auth/change, сессия обновляется) +
  группы «Безопасность» и «Система»; ссылка на /admin оставлена
  (журналы, аудит, даталоггер — там);
- **ПАЗ** и **Система** (OTA/бэкап/ребут) — уже соответствовали эталону,
  не тронуты. Мастер-ключ/карты/кнопка выхода — чужой профиль, не перенесены.
Правки: wx.html (стили панелей, 3 вкладки, JS-движок ~150 строк,
ensureConfig в showTab) + версия в WeatherGateApp.h. Ядро не тронуто.
Ворота: host 43751 PASS / 0 FAIL; JS-синтаксис node --check OK; прогон
4 профилей: weather_gate 1085686 Б (версия 0.4.0 подтверждена strings
в бинаре), test_rig 1059078, smart_lock 1095530, home_master 1096960.
ФС: mklittlefs -s 1441792, round-trip 3/3 MATCH (wx.html 52819→63266 Б).
version.json: fw_md5 f285ae28aa7f135ecce4be56ae8d0174,
fs_md5 b44333d500f181a74f94f6a55b694c85.
Приёмка за владельцем: на вкладке «Сеть» — поля сети/MQTT читаются,
изменение сохраняется (тост «Сохранено»), DHCP прячет статику; «Радио» —
профильные поля (частота, AGC, сторожа); «Админ» — смена пароля и группы
Безопасность/Система; секрет брокера пустым не затирается.
Далее: W3.3 — сканер лучшей частоты (дизайн-нота).

**0.4.1 — человеческие имена AGC + «Тишина эфира» в ПАЗ (03.09.2026,
запрос владельца):** во вкладке «Радио» поля wx.rf_agcctrl1/2 подписаны
по-человечески: «AGC: защита от перегрузки (AGCCTRL1, дефолт 71)» и
«AGC: усиление входа LNA (AGCCTRL2, дефолт 27)» (FIELD_UI_LABEL — подмена
только в подписи виджета). «Тишина эфира -> тревога, мин» (wx.silence_min)
рисуется во вкладке ПАЗ карточкой «Сторожа эфира». Архитектурно важно:
поле ОСТАЛОСЬ в группе схемы «Телеметрия и сторожа» — перенос между
группами сдвинул бы индексы и сломал сохранённые значения (правило
JSON-конфига); переезд чисто панельный (FIELD_TAB_HOME + EXTRA_TAB_GROUPS),
как панельные преобразования единиц у замка. Правки: только wx.html +
версия. Ворота: JS node --check OK; host 43751 PASS / 0 FAIL; прогон
4 профилей зелёный (0.4.1 подтверждена strings); ФС round-trip 3/3 MATCH.
version.json: fw_md5 307bbca9750d20a589a01359593a1f94,
fs_md5 d72cbf9c354e189ff19def158f129922.
Приёмка: «Радио» — AGC-поля с человеческими именами, значения 71/27
читаются и сохраняются; «ПАЗ» — карточка «Сторожа эфира» с полем тишины;
в «Радио» это поле больше не дублируется.

**ЗАКОН ФЛОТА (утверждён владельцем 03.09.2026):** ядро неизменно для
любого профиля. Все отображения, переименования и «переезды» полей/данных —
только на пользовательском уровне, в локальной веб-страничке профиля
(FIELD_UI_LABEL, FIELD_TAB_HOME, панельные преобразования единиц).
Схема конфига, индексы полей, NVS, API и глобальная архитектура при этом
не затрагиваются никогда. Запросы на изменение ядра — только через
Issue #1. Этот закон — основа правила 23 и всех панельных правок 0.4.x.

**W3.3 — инвентаризация и дизайн-нота (03.09.2026):** всё необходимое
уже есть в ядре 5.8.4 — setFreqMHz/readRssiNow публичны и проверены
probe=1/2, readback регистров — probe=3. **Запрос в Issue НЕ нужен**:
сканер целиком профильный (закон флота). Подсказка в wx.html про
«нужны setFreqMHz/readRssiNow» устарела — удалим в реализации.
Дизайн-нота выдана владельцу: два режима (быстрый — карта эфира ~1 мин;
полный — по пакетам станции, 21–41 точка × 60 с), неблокирующая машина
состояний, инвариант возврата рабочей частоты с readback, пауза сторожа
тишины на время прогона, изоляция dup/pkt нормы ПАЗ, UI в «Радио»,
новых полей конфига — 0. Ждём утверждения владельцем → реализация.

**0.5.0 — W3.3, сканер лучшей частоты (03.09.2026, нота одобрена
владельцем):** реализация по дизайн-ноте + наследие монолита v5.2
(окно ±0.20 МГц — АЧХ 914.80–915.20; оптимум датчика там был 915.00–
915.04; из radio_scanner.pdf: полоса 325 кГц, суммарный уход кварцев
±50–70 кГц — окно покрывает с запасом). Ядро не тронуто (закон флота):
всё на публичном API драйвера (setFreqMHz/readRssiNow/packetSeq).
- **WgScanCore.h** (новый, профильный): чистая логика — сетка (шаг
  0.02/0.05, кламп к 914–916, без дублей), агрегация точек (pkt/RSSI
  max/avg/шум), рекомендация (max rssiMax среди pkt≥1, ничья — ближе
  к домашней, пусто — нет рекомендации). Урок хода: сотые МГц НЕ
  влезают в uint16_t (91500 > 65535) — частоты на uint32_t, поймано
  host-тестами до железа (8 FAIL → фикс → зелень).
- **Прошивка**: машина состояний из tick() (1 с): шум 1 выборка/с,
  пакеты — дельта packetSeq (серия 6 копий дедуплицирована драйвером),
  перестройка по окну dwell, финиш/отмена — возврат на рабочую частоту
  с readback в журнал (урок №23). Сторож тишины на паузе во время
  прогона (WgRadioCheck: scanActive → ok). Endpoint'ы: scan=status
  публичный; start/abort/tune — только админ (404 неадмину).
- **UI «Радио»**: карточка сканера — шаг/окно, старт/отмена, прогресс
  с ETA, SVG-график (сплошная — max сигнала, пунктир — шум, маркер —
  рабочая частота, клик по точке — живая перестройка без записи
  конфига), таблица точек, кнопка «Применить» (пишет wx.rf_freq_mhz
  штатным POST /api/config + живая перестройка). Устаревшая подсказка
  про «нужны setFreqMHz/readRssiNow» удалена.
Ворота: host 43816 PASS / 0 FAIL (+65, норма выросла); JS node --check
OK; прогон 4 профилей: weather_gate 1089886 Б (+4200), test_rig
1059078, smart_lock 1095530, home_master 1096960; 0.5.0 в бинаре
(strings). ФС round-trip 3/3 MATCH. version.json: fw_md5
6dffb62253d5764c7a621acfc1095233, fs_md5 d526b662d456431e0a578b25b385e291.
Приёмка за владельцем: старт прогона (21 точка × 60 с ≈ 21 мин) —
прогресс идёт, точки заполняются, на соседних с рабочей пакеты ловятся;
по финишу — возврат на 915.00 (probe=3 readback), сторож тишины молчал,
рекомендация подсвечена; «Применить» пишет конфиг; клик по точке —
живая перестройка. ПАЗ dup/pkt не размыт (scan-пакеты — обычные приёмы
на точках, в глобальные счётчики идут как штатные).

## 04.09.2026 — weather_gate 0.5.1: правило 23 п.3 (setHiddenGroups) по заданию ядра

Задание ядерной ветки от 04.09.2026 (правило 23 для weather_gate) разобрано:
п.1 (порядок вкладок) закрыт в 0.3.10, п.2 (hg-группы не отображаем) —
whitelist TAB_GROUPS + страховка hg-пропуска в 0.5.1, п.4 (полнота
покрытия панелями) — 0.4.0. Оставался п.3: setHiddenGroups.

Подтверждено чтением ядра: Kernel.cpp регистрирует ScheduleService,
CounterService и AudioService безусловно → группы «Планировщик»,
«Счётчики», «Звук» — сироты шлюза (нет SCHED_EVENT, счётчиков, DFPlayer).

Изменения (один файл — одна правка, ядро не тронуто):
- **WeatherGateApp.cpp**: registerExtensions() первой строкой —
  `ConfigService::getInstance().setHiddenGroups("Планировщик,Счётчики,Звук");`
  (образец — SmartLockApp).
- **wx.html**: в renderConfigAll страховка `if(fields[0].f.hg) continue;`
  — даже если whitelist расширят, hg-группы в профильных панелях не
  появятся никогда. /admin показывает всё, как и прежде.
- Версия → 0.5.1.

Ворота: JS node --check OK; host 43816 PASS / 0 FAIL (норма не упала);
прогон 4 профилей: weather_gate 1090018 Б (+132 к 0.5.0, «0.5.1» в
strings), test_rig 1059078, smart_lock 1095530 (совпал с отпечатком
ядра), home_master 1096960 (после полной чистки битого кэша
/root/.cache/arduino — частичной не хватило). ФС round-trip 3/3 MATCH.
version.json: fw_md5 783c87991b08400e5375110d45dd5faa, fs_md5
dd137276aeccd05105690ef2d24184a5.

Приёмка за владельцем: прошивка ставит hg-флаги + ФС рендерит (пара!),
вкладки Радио/Админ без групп-сирот, /admin — полный набор.

## 04.09.2026 — приёмка 0.5.1 на железе: ЗЕЛЁНАЯ (владелец)

Прошита пара (прошивка + ФС). Проверено владельцем:
- Сканер лучшей частоты (0.5.0): полный прогон 21 точка × 60 с,
  «Анализ завершён. Частота возвращена на рабочую (915.00 МГц)».
  Острого пика нет — АЧХ пологая, как и ожидалось при полосе приёма
  325 кГц (наследие монолита v5.2 подтверждено). Пакеты ловятся
  на 914.88–915.06, рекомендация 915.02 МГц (-85 дБм max/avg, шум
  -89 дБм) — в пределах одного шага 0.02 от рабочей 915.00.
  Вывод: текущая частота уже оптимальна, перестройка не требуется
  (кнопка «Применить» есть, но выигрыш в пределах погрешности).
  Сторож тишины во время прогона молчал (пауза через scanActive).
- Сеть — зелёный: панели Сеть/MQTT на месте, DHCP-видимость работает.
- ПАЗ — зелёный: ЗДОРОВА, предупреждения 5/0, куча 67 КБ, сторожевой
  взведён, проверки wg.bme280 OK, wg.radio OK. Групп-сирот
  («Планировщик», «Счётчики», «Звук») в панелях нет — hg работает.
- Графики рисуются (давление у.м., 6 ч — тренд виден).
- Данные текут: RSSI -83…-85 дБм, пакет 18–30 с назад, батарея норма,
  BME280 27.5 °C; в HA сенсоры обновляются (MQTT-мост жив, версия 5.8.4).
Общий статус флота — зелёный. Приёмка 0.5.0 + 0.5.1 ПРИНЯТА.

## 04.09.2026 — ответ ядерной ветки: 0.5.1 зачтена, FIELD_TAB_HOME = правило 23-а

Ядерная ветка ответила (Issue #4 — новая профильная ветка weather_gate
на GitHub, по аналогии со smart_counter; Issue #2 обновлён):
- Задание правила 23 закрыто полностью, 0.5.1 ЗАЧТЕНА. Страховка
  hg-пропуска отмечена как хорошая практика (двойная защита).
- «Нехарактерное размещение» полей ПРИНЯТО как дополнение к правилу 23 —
  **правило 23-а (размещение по смыслу)**, флот-стандарт. Ядерная ветка
  фиксирует его в дорожной карте и брифингах профильных веток
  (smart_counter получит сразу).

Границы правила 23-а (жёсткие, нарушение 1–2 = дефект приёмки):
1. Только панельный уровень: схема, группы, индексы, NVS, API, /admin
   неизменны; «дом» задаётся только маппингом FIELD_TAB_HOME в wx.html.
2. Одно поле — один дом: каждое не-hg поле видимо ровно в одном месте
   закрытой зоны (полнота покрытия сохраняется), без дублей и потерь.
3. Комментарий у маппинга: какое поле, из какой группы, почему здесь
   (наш комментарий у wx.silence_min — образец).
4. Приём соразмерности: перенос — исключение для полей с очевидным
   «смысловым местом» вне группы схемы; массовые перекладки = повод
   пересмотреть схему групп через ядерную ветку, а не панельно.

Решение владельца по сканеру: рабочая частота остаётся 915.00 МГц
(рекомендация 915.02 в пределах одного шага, выигрыш сомнителен —
частота подтверждена 72-часовым soak). «Применить» не использовалась.

Дальше — по дорожной карте.

## 04.09.2026 — weather_gate 0.5.2: wx.diag погашен по умолчанию

Отложенный пункт «решение по wx.diag» закрыт по рекомендации — гасим.
Расследования W3.2 (404 графиков, заморозка RSSI, висячий tail ядра)
закрыты, soak 72 ч зелёный — расширенный лог выполнил задачу и шумел.

Приём (архитектурно чистый, закон флота соблюдён): поле, его индекс и
группа «Телеметрия и сторожа» СОХРАНЕНЫ — изменён только дефолт схемы
"1" → "0". Индексы полей не сдвинуты, NVS/JSON не затронуты, механизм
s_diagLog остаётся в коде: при надобности диагностика включается
тумблером из /admin или панели «Радио» без перепрошивки. Если на
устройстве значение уже сохранено как «1» — выключить один раз
тумблером (сохранённое значение перекрывает дефолт схемы).

Ворота: host 43816 PASS / 0 FAIL; прогон 4 профилей: weather_gate
1090018 Б (совпал с 0.5.1 — правка односимвольная, «0.5.2» в strings),
test_rig 1059078, smart_lock 1095530 (отпечаток ядра), home_master
1096960. Урок повторён: после переустановки платформы stale-кэш дал
фантомный Wire.h — лечится только полным rm -rf /root/.cache/arduino
(бинарь weather_gate предварительно сохранён). ФС round-trip 3/3.
version.json: fw_md5 c3b96c5ed540d1b3fdda4f4b9a949797, fs_md5
f2ee875b0e8f2f05e7b5933f3679d78c.

## 04.09.2026 — weather_gate 0.5.3: трассировка запросов свёрнута в «Систему»

По итогам приёмки 0.5.2 владельцем: сетевая трассировка (diag3/diag4,
клиентская, последние 16 запросов) — инструмент этапа отладки, в
постоянной эксплуатации место занимает.

Изменения (только wx.html, панельный уровень):
- модал входа: блок трассировки убран СОВСЕМ (traceRowsLogin удалён);
- вкладка «Админ»: блок трассировки убран;
- вкладка «Система»: трассировка живёт в свёрнутом <details>
  («🖥️ Трассировка запросов», по умолчанию закрыт — как системная
  информация для этапа отладки, по образцу техжурнала замка);
- renderTrace: ссылка на traceRowsLogin удалена.
Логика трассировки и мягкой проверки 401 (diag4) сохранена — меняется
только отображение.

Аудит: владелец подтвердил — отключён в ядерной /admin (не наш уровень,
состояние устройства).

Ворота: JS node --check OK; host 43816 PASS / 0 FAIL; прогон 4
профилей: weather_gate 1090018 Б («0.5.3» в strings), test_rig
1059078, smart_lock 1095530, home_master 1096960. ФС round-trip 3/3.
version.json: fw_md5 bdc7375df4ca4f0eef313dff5c95f272, fs_md5
11c4991e5f930ed209e072991a6af8dd.

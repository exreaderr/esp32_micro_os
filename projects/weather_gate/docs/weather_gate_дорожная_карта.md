# weather_gate — дорожная карта проекта

Ведётся по ходу стадий W1–W5. Статусы: ✅ сделано, 🔧 в работе, 📋 запланировано, 💤 отложено (опция).

## Стадии

| Стадия | Содержание | Статус |
|---|---|---|
| W1 | Скелет профиля, Bme280Driver (BME280/BMP280, автодетект 0x76/0x77), конфиг wx.*, conformance | ✅ host 43598 PASS |
| W2 | Cc1101Driver (строго RX-only), декодер Fine Offset WH1080 (PCM 58 мкс, rtl_433-эталон), дедупликация пары 31 мс | ✅ host 43719 PASS |
| W3 | WeatherCore (feels-like, дождь, state), weather-JSON (HTTP + MQTT retained) для smart_lock, DataLog + uPlot-панель /web/wx.html, ПАЗ-проверки wg.bme280/wg.radio, авто-высота по координатам | ✅ host 43745 PASS, сборка 4/4 профилей |
| W4 | Зеркало даталога wx_* на home_master — **реализовано ядерной веткой** (WeatherMirror, релиз 5.8.2; контракт weatherJson подтверждён полем) | ✅ код; стенд — на владельце; релиз 5.8.2 после бенча зеркала |
| W5 | **Слоёный прогноз** (концепция одобрена владельцем 23.08): L1 — Замбретти + 5-классовый баротренд, офлайн всегда; шторм-предупреждение (>4 гПа/3 ч) событием на шину + warning в ПАЗ. L2 — внешний прогноз Open-Meteo, только FULL, с честной подписью источника. Плюс backfill зеркала и soak 72 ч | 📋 |
| W6+ | **Слой 3 — локальная ML-модель** на собственном архиве DataLog: обучение офлайн (ПК/мастер), крошечный инференс (регрессия/дерево). Триггер: накоплен архив за сезоны | 💤 стратегия |

## Опции (отложено решением владельца)

### 💤 Датчики среды технического помещения (котельной)
**Решение владельца (W3):** шлюз стоит в котельной — температура там всегда
повышена, влажность понижена; комнатные SHT31/VEML7700 смысла не имеют.
Зафиксировано как опция на будущее, в активный план не входит.
- **Триггер возврата:** появление задачи контроля микроклимата самой
  котельной (перегрев помещения, протечки → тогда кандидаты: SHT31 0x44
  на системной I2C 32/33, датчик протечки на свободный GPIO 5/12).
- **Стоимость входа:** драйвер по образцу Bme280Driver + поля конфига в
  конец схемы + host-тесты; ядро не затрагивается.

## OTA на погодном шлюзе (W3 fix2, 23.08.2026)

- **Запрос владельца:** OTA на шлюзе, как на мастере и замке (эталон —
  веб-страница smart_lock). Актуально при стендовых итерациях.
- **Анализ:** вся OTA-механика — ядерная (UpdateService: A/B-разделы с
  автооткатом, валидация 60 с, манифест на HA, фоновая докачка в tick,
  журнал истории, NVS-replay) и уже работает на шлюзе (19 модулей,
  UpdateService в списке, /api/ota/* под requireAdmin). Профилю нужна
  только панель.
- **Сделано (только UI, ядро и App не тронуты):** карточка «Система» в
  /web/wx.html после входа администратора — сведения OTA, проверка
  манифеста, обновление с сервера с следилкой прогресса, ручная загрузка
  .bin прошивки и ФС с прогресс-баром (предупреждение: ФС стирает
  даталог), NVS-бэкап/восстановление конфигурации, перезагрузка,
  ссылка на ядерную /admin. Версия профиля 0.3.0 → **0.3.1**.
- **Ворота:** host 43745/0 + ядерный 43351/0; сборка 4/4 —
  weather_gate 1075734 B (байт-в-байт fix1), соседние без изменений.
- **Развёртывание:** первый раз ФС с новой панелью — по кабелю (как
  заливался lock.html); дальше всё по воздуху.

## W5 принят ядром (Issue #1, 23.08.2026)

- Интерфейс `wxc::zambretti(pressSeaHpa, trend3hHpa, windDirDeg, summer)
  -> {letter, trend}` — **согласован**. Тренд из своего DataLog wx_p,
  сезон из RTC, `wx.zamb_en` в конец схемы — по доктрине.
- **Ядро ЗАРЕЗЕРВИРОВАЛО:** слот в реестре AuditService под
  `WG_EVENT_STORM_WARNING` + метрику HealthMonitor `wg.storm`
  (порог >4 гПа/3 ч). Слот наш, публикуем в реализации W5.
- Слой 2 (Open-Meteo): принят, только FULL, `wx.fcst_en=1` по умолчанию.
- Backfill: по умолчанию ВЫКЛ (`wx.mirror_backfill 0`); MVP — retained +
  публичный /api/dev/weather. Исторический backfill — не раньше 5.8.3,
  по отдельному решению владельца.
- Слой 3 (ML на P4): вектор зафиксирован, интерфейсов не создаём.
- **Soak 72 ч — общий протокол принят:** uptime, reboots, heap watermark,
  pkt/dup/crc_err, тиры DataLog, MQTT-переподключения, retry
  altitudeTask, стабильность HealthMonitor. **Мастер и замок обновлены
  до 5.8.2 и ушли на параллельный стенд** (владелец, 23.08); результаты
  сводим в Issue #1.

## Стратегическое будущее: аналитический узел на ESP32-P4

- **Планы владельца (23.08.2026):** мастер переезжает с ESP32-S3-ETH
  (SD 32 ГБ) на **ESP32-P4-WIFI6-POE-ETH** + апгрейд флеша до 32 МБ.
- P4: двухъядерный RISC-V 400 МГц, векторные AI-инструкции; радио
  на модуле через companion-чип. С SD и зеркалом wx_* мастер становится
  **озером данных** глубиной сезоны/год.
- Архитектурная роль: шлюз остаётся сенсорным краем (слои 1–2),
  мастер на P4 — аналитическим узлом слоя 3: хранение глубокого архива,
  обучение/подбор модели, раздача уточнённого прогноза обратно.
  МикроОС-философия сохраняется: при отказе аналитики край остаётся
  на слое 1 и не тупеет ни на грамм.
- Это мысли на будущее — в активный план не входит, триггер: приход
  железа P4 + накопленный архив.

## Кандидаты в ядерную ветку (запросы от профиля)

1. **library.properties: `depends=SPI,Wire`** (W3). Драйверы
   src/drivers/ безусловно включают `<SPI.h>`/`<Wire.h>`; без `depends`
   arduino-cli не даёт include-пути в профилях, где SPI не подтягивается
   ничем ещё (поймано сборкой test_rig). Однострочное изменение.
   **Статус: ПРИНЯТО ядерной веткой (Issue #1, п.3.1) — войдёт в 5.8.2.**
   Шаблонный library.properties из зоны профиля удалён (п.5 Issue #1).
2. Урок W1: кламп отрицательного давления в compensatePress (край ADC →
   wrap int64→uint32 давал ~16,7 МПа). Принят к сведению (Issue #1, п.3.2).

## Синхронизация с ядерной веткой (Issue #1, 18–19.08.2026)

- Единый репозиторий: `exreaderr/esp32_micro_os` (ядро 5.8.1,
  каноничная раскладка: `MicroOS/` + `projects/` — соседи). Канал
  связи — Issues основного репозитория.
- Ревью W1–W3: **приняты** после устранения блокера.
- **Блокер (п.4) УСТРАНЁН:** Cc1101Driver больше не включает
  WeatherGateProfile.h — пины параметром (`Cc1101Pins` + `configurePins()`
  до init, без configurePins — честный отказ); пины для ISR — файловые
  статики; инклуды ядра в драйверах переведены на угловые
  (переносимость между раскладками и будущий мерж в MicroOS/src/drivers).
- **W4 СОГЛАСОВАН (п.3.3):** модуль-зеркало даталога — на стороне
  home_master (retained MQTT /weather или опрос HTTP — выбор при
  реализации). Код gate не меняется; gate самодостаточен.
- Верификация после правок: host 43745 PASS / 0 FAIL; сборка
  weather_gate OK (1075734 B flash); контроль блокера — smart_lock /
  home_master / test_rig собраны с ИЗЪЯТЫМ из дерева
  projects/weather_gate (smart_lock байт-в-байт 1093730 B — их
  релизная цифра 5.8.1).

## Повторная приёмка и релиз 5.8.2 (19.08.2026)

- **W1–W3 fix1 — ПРИНЯТО** (комментарий ядерной ветки в Issue #1):
  блокер закрыт эмпирически, host 43745/0, сборка 4/4 из main.
  Расхождение flash −16 B от моих 1075734 при идентичных globals —
  шум путей сборки, зафиксировано для протокола с их стороны.
- **Релиз 5.8.2** содержит: `depends=SPI,Wire` (мой запрос п.3.1),
  WeatherMirror (W4) в home_master, мой fix1 байт-в-байт.
- **WeatherMirror (W4)** реализован по контракту weatherJson 1:1
  (проверено по исходнику: valid/temp/feels_like/state/humidity/wind/
  gust/dir/rain/press| null/press_sea/rssi/batt/age_s; batt: 1=ОК, 0=
  разряжена). Два канала приёма: subscribeExternal (норма) + хук
  локального брокера (автономия), дедуп по телу, каналы DataLog с теми
  же именами wx_ot/wx_oh/wx_p/wx_w/wx_r.
- Моя верификация 5.8.2 (повторная, со своей стороны): профильный
  host-сьют 43745 PASS / 0 FAIL; ядерный 43351 PASS / 0 FAIL; сборка
  4/4 профилей (weather_gate 1075734 B, smart_lock 1093730 B
  байт-в-байт, home_master 1123394 B, test_rig 1057770 B).
## Мердж драйверов (Issue #1, 20.08.2026)

- **П.3 ВЫПОЛНЕН ядерной веткой:** Bme280/Cc1101 + Core-логики живут в
  MicroOS/src/drivers/; профиль переключён на библиотечные драйверы
  (угловые инклуды, локальные src/drivers удалены). Пакет возвращается
  через владельца — приёмка с полным прогоном ворот после получения.
- Их ворота D3: host 43351+43745 PASS; сборка 4/4, weather_gate
  1075718 B — байт-в-байт fix1, соседние профили без изменений;
  мой host-сьют адаптирован ими к библиотечным путям (run_tests.sh
  работает как прежде).
- **Релиз 5.8.2 — после бенча зеркала (W4) на стенде.** W5 (Замбретти,
  backfill, soak 72 ч) объявлен следующим фронтом, ядро ждёт предложения
  по интерфейсу.

## Эррата платы WT32-ETH01: IO5/IO35

- **Прозвонка владельцем (20.08.2026, несколько плат):** пэд,
  подписанный шелкографией «IO35», — это фактический **GPIO35**
  (input-only, без подтяжек) — как и принято в пин-плане (CC1101 MISO).
- **Источник разночтения (расследование владельца):** опечатка в ранних
  официальных схемах Wireless-Tag (контакт назвали IO5, не имея
  отношения к GPIO5), разошедшаяся по неофициальным распиновкам.
  С версии платы v1.2 шелкография исправлена на IO35.
- **Свойства GPIO35:** строго вход (input-only), внутренних подтяжек
  в кристалле нет. Для CC1101 MISO — идеальное совпадение роли:
  линию активно драйвит трансивер, подтяжка не нужна.
- **Настоящий GPIO5** выведен на пэд **CFG** — учитывать, если он
  понадобится (опция датчиков котельной упоминает GPIO 5/12 для
  протечки: подтяжка у GPIO5 есть, валиден).
- **Изменений в коде и пин-плане НЕ требуется.** При сборке ориентир —
  фактическая прозвонка, а не надпись на плате или старые схемы.

## Стенд владельца (23.08.2026)

- **Шлюз в работе:** прошивка 5.8.2 (build ee57070), id d4e9f4bd612b,
  ETH 10.146.75.55, MQTT на встроенный брокер home_master (10.146.75.54).
- Радио — временный модуль E07-M1101D (основной ждёт плату-переходник);
  RSSI −108 дБм на времянке, внимания не обращаем до основного модуля.
- **Монитор порта — чисто:** 19 модулей, 4/4 драйвера init ok (cc1101
  включительно), конформанс 14 PASS / 0 FAIL на железе, RTC→NTP
  синхронизация, 5 каналов DataLog wx_* подняты.
- **Сверка с консолью WH1080:** давление BME280 и влажность — копейка
  в копейку; остальное идёт с уличного блока и живо обновляется в панели.
- Панель /web/wx.html живая (автообновление 30 с, карточка «Сейчас»).
- Монтажная находка: перепутанные SDA/SCL давали отказ DS3231+BME280 —
  штатный путь отлова: critical wg.bme280 + невалидное время. Исправлено.
- **ПАЗ — боевая проверка пройдена (владелец):** BME280_LOST (critical)
  на перепутанной шине → локализация по сообщениям сторожа → после
  перепайки самовосстановление critical→ok без перезагрузки. Переходы
  состояний работают как задумано.
- Координаты зафиксированы, высота над уровнем моря введена вручную
  (точнее авто-высоты по модели ~90 м); press_sea считается честно.
- **Стенд переведён в режим накопления статистики** (сверка 24 ч,
  далее бенч зеркала W4).
- **Косметика в бэклог:** имена каналов «Улица, температура/влажность»
  режутся по 28-байтному бюджету посередине кириллического символа —
  сократить названия при следующем пакете (профильная правка).
- Стороннее наблюдение (не наш профиль): у smart_lock в журнале мастера
  CPU_TEMP_CRITICAL 85,0 °C — передать ядерной ветке/проверить корпус.
- Далее: сверка приёма 24 ч, бенч зеркала W4 (каналы wx_* мастера vs
  шлюза), проверка авто-высоты (wx.lat/lon → wx.altitude_m).

## Уроки (нумерованные)

- **№12 (W2):** контрольные точки частотного слова CC1101 считать в той же
  арифметике, что и целевой код (float32 против double — 1 LSB).
- **№13 (W3):** host-тесты не покрывают App/Profile — только Core.
  Компиляционный прогон всех профилей arduino-cli обязателен до выдачи
  (поймано: регистрация Cc1101Driver без include; LogLevel::ERROR вместо
  LogLevel::Error; NetworkManager вместо NetworkService).
- **№14 (W3):** arduino-cli добавляет include-путь библиотеки, только если
  её заголовок виден в цепочке скетча; драйверы в общей src/drivers —
  повод держать `depends` в library.properties актуальным.
- **№15 (W3-fix, Issue #1):** общий драйвер НЕ включает заголовки профиля —
  зависимости передаются параметрами (configurePins до init). Относительные
  quoted-инклуды в переносимых файлах — мина: резолвятся «на глубину»
  каталога и тихо ломаются при смене раскладки; для переносимых драйверов —
  только угловые инклуды ядра. Контрольный тест: сборка соседних профилей
  с ИЗЪЯТЫМ из дерева собственным профилем.
